{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"coshop","text":"<p>coshop is a benchmark for evaluating conversational shopping assistant agents that must elicit latent user preferences through dialogue and retrieve the correct item(s) from a catalog.</p>"},{"location":"#subpackages","title":"Subpackages","text":"Package Description <code>coshop.data</code> Dataset loading, item representation, and utility scoring <code>coshop.evaluation</code> NDCG-based metrics and per-turn budget tracking <code>coshop.tools</code> LangChain-compatible retrieval and history-search tools <code>coshop.user_simulator</code> Simulated users that reveal preferences progressively <code>coshop.utils</code> Shared data types, agent base classes, and helper utilities"},{"location":"api/data/datasets/","title":"Datasets","text":"<p>The base <code>Dataset</code> and <code>Specification</code> classes.</p> <p>Core dataset abstractions for the coshop benchmark.</p> <p>This module defines two primary classes:</p> <ul> <li>:class:<code>Specification</code> \u2014 a single benchmark task, containing the ground-truth   target item(s) (<code>xstar</code>), the layered preference descriptions (z-variants),   the SEC feature split, and utility scoring functions.</li> <li>:class:<code>Dataset</code> \u2014 an abstract, lazily-loaded collection of   :class:<code>Specification</code> objects backed by a shared item catalog.</li> </ul> <p>Helper function :func:<code>build_z_variants_for_spec</code> constructs the z-variant strings used by simulated users and baseline evaluations.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset","title":"<code>Dataset(max_xstar=1, restrict_representation_to_simulator_columns=True, max_search_features=None, dropout_extra_search=True, true_features=None, hidden_features=None, filterable_features=None, catalog=None, popularity_df=None, identifying_cols=None)</code>","text":"<p>Abstract base class for a coshop benchmark dataset.</p> <p>A Dataset is an iterable collection of :class:<code>Specification</code> objects backed by a shared item catalog.  Specs are loaded lazily by default; call :meth:<code>load_specs</code> to pre-load a batch.</p> Subclasses must implement <ul> <li>:attr:<code>dataset_name</code> (property)</li> <li>:attr:<code>dataset_description</code> (property)</li> <li>:attr:<code>item_name</code> (property)</li> <li>:attr:<code>special_match_functions</code> (property)</li> <li>:meth:<code>_load_specs</code></li> <li>:meth:<code>get_image_fn</code></li> <li>:meth:<code>render_item_fn</code></li> </ul> <p>Initialise a Dataset.</p> <p>Parameters:</p> Name Type Description Default <code>max_xstar</code> <code>Optional[int]</code> <p>Maximum number of acceptable ground-truth items per spec.  Specs with more ground-truth items are discarded. Defaults to <code>1</code>.</p> <code>1</code> <code>restrict_representation_to_simulator_columns</code> <code>bool</code> <p>When <code>True</code> (default), the item representation shown to the agent only includes columns visible to the simulator (i.e. excluding <code>hidden_features</code>).  Set to <code>False</code> to expose all catalog columns.</p> <code>True</code> <code>max_search_features</code> <code>Optional[int]</code> <p>Optional cap on the number of search- category features available per spec.  Works together with <code>dropout_extra_search</code> to modify the effective SEC split. <code>None</code> disables the cap.</p> <code>None</code> <code>dropout_extra_search</code> <code>bool</code> <p>Controls what happens to search features that exceed <code>max_search_features</code>.  When <code>True</code> (default) the extra features are masked to <code>OPEN_TO_ANYTHING</code> in the simulator's view (i.e. dropped entirely).  When <code>False</code> they are re-categorised as experience features instead.</p> <code>True</code> <code>true_features</code> <code>Optional[Dict[str, str]]</code> <p>Mapping from column name to human-readable feature description used by the item representation.  May be renamed to <code>feature_descriptions</code> in a future release.</p> <code>None</code> <code>hidden_features</code> <code>Optional[List[str]]</code> <p>Column names that should not be visible to the user simulator.  These columns are excluded from the simulator catalog and the restricted item representation.</p> <code>None</code> <code>filterable_features</code> <code>Optional[List[str]]</code> <p>Column names on which the agent's retrieval tools may apply hard equality filters.</p> <code>None</code> <code>catalog</code> <code>Optional[DataFrame]</code> <p>Pre-loaded catalog DataFrame.  Subclasses normally pass this after loading from disk.</p> <code>None</code> <code>popularity_df</code> <code>Optional[DataFrame]</code> <p>Optional DataFrame mapping item IDs to popularity scores, used by the popularity-ordered retrieval mode.</p> <code>None</code> <code>identifying_cols</code> <code>Optional[List[str]]</code> <p>Columns (beyond <code>\"id\"</code>) that uniquely identify an item and should always appear in representations (e.g. <code>[\"title\", \"artist\"]</code>).</p> <code>None</code>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.dataset_description","title":"<code>dataset_description</code>  <code>property</code>","text":"<p>One-sentence description of the dataset shown in documentation and UIs.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.dataset_name","title":"<code>dataset_name</code>  <code>property</code>","text":"<p>Programmatic identifier for this dataset (e.g. <code>\"hm\"</code>).</p> <p>Used to route API requests and locate dataset-specific asset files.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.item_name","title":"<code>item_name</code>  <code>property</code>","text":"<p>Singular noun for items in this catalog (e.g. <code>\"clothing item\"</code>, <code>\"movie\"</code>, <code>\"book\"</code>).</p> <p>Used to build natural-language prompts for the user simulator.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.special_match_functions","title":"<code>special_match_functions</code>  <code>property</code>","text":"<p>Column-level match-function overrides for the utility scorer.</p> <p>Returns a dict mapping column name to the name of the match function it requires (e.g. <code>\"jaccard_similarity\"</code> for multi-valued tags, or a numeric comparison for price ranges).  Columns not listed use exact match.  Passed to :class:<code>~coshop.data.utility.ColumnMatchingUtilityFunction</code>.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.get_initial_known_features_for_spec","title":"<code>get_initial_known_features_for_spec(index)</code>","text":"<p>Load initial known features for a fixed spec from data//assets/incremental_search_recall_results_new.csv. Same logic as in expert_user._load_initial_features_from_incremental: resolve feature_names (direct column names and \"tag:foo\") to the visible simulator columns (including exploded tag column names)."},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.get_vector_search_baselines_for_spec","title":"<code>get_vector_search_baselines_for_spec(index)</code>","text":"<p>Return pre-computed vector-search baseline data for a spec.</p> <p>Lazily loads <code>data/&lt;dataset_name&gt;/assets/baseline_queries.json</code> on first call and caches the result.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Spec index (zero-based within the dataset split).</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>A dict with keys <code>\"z0\"</code> and <code>\"zstar\"</code>, each mapping to</p> <code>Dict[str, Any]</code> <p><code>{\"query\": str, \"top_k_ids\": [str, ...]}</code>.  Returns <code>{}</code> if</p> <code>Dict[str, Any]</code> <p>no baseline data is available for the given index.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.load_specs","title":"<code>load_specs(indexes=None, reload=False)</code>","text":"<p>Pre-load a batch of specs into memory.</p> <p>Parameters:</p> Name Type Description Default <code>indexes</code> <code>Optional[List[int]]</code> <p>List of spec indices to load.  Defaults to all indices in the current split.</p> <code>None</code> <code>reload</code> <code>bool</code> <p>When <code>True</code>, reload specs that are already cached. Defaults to <code>False</code>.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any requested index does not belong to the current dataset split.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Dataset.render_item_fn","title":"<code>render_item_fn(id, show_features=True, *, return_html=False, **kwargs)</code>","text":"<p>Render a catalog item for display.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>str</code> <p>Catalog item ID.</p> required <code>show_features</code> <code>bool</code> <p>Whether to include feature text alongside the image.</p> <code>True</code> <code>return_html</code> <code>bool</code> <p>When <code>True</code>, return an HTML-serialisable payload dict <code>{\"title\": str, \"body_html\": str, \"image_url\": str}</code> suitable for embedding in an iframe instead of rendering directly in Streamlit.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Additional keyword arguments passed to the underlying render implementation.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Any</code> <p>Streamlit render output, or an HTML payload dict when</p> <code>Any</code> <p><code>return_html=True</code>.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Specification","title":"<code>Specification(dataset_name, index, version=None, item_name=None, z0=None, zstar=None, baseline_queries=None, name=None, state_files=None, files_to_clean=None, xstar=None, xstar_series=None, xstar_simulator_view=None, sec_split=None, initial_known_features=None, simulator_persona=None, historical_data=None, historical_ids=None, historical_ratings=None, catalog=None, column_descriptions=None, special_match_functions=None, representation=None, z0_baseline_query=None, zstar_baseline_query=None, z0_baseline_ids=None, zstar_baseline_ids=None, **kwargs)</code>","text":"<p>A single benchmark task within a :class:<code>Dataset</code>.</p> <p>A Specification bundles everything needed to run one elicitation episode: the ground-truth target item(s) (<code>xstar</code>), layered user-preference descriptions (z-variants), the SEC feature partition, historical user data, pre-computed baseline queries, and callable utility functions.</p> <p>Attributes:</p> Name Type Description <code>dataset_name</code> <p>Identifier of the parent dataset (e.g. <code>\"hm\"</code>).</p> <code>index</code> <p>Position of this spec within the dataset.</p> <code>item_name</code> <p>Human-readable singular noun for catalog items (e.g. <code>\"clothing item\"</code>).</p> <code>z0</code> <p>Preference description from initially known features only.</p> <code>zs</code> <p>Preference description after all search features are revealed.</p> <code>zse</code> <p>Preference description after search + experience features are revealed.</p> <code>zstar</code> <p>Full ground-truth preference description (search + experience + credence features).</p> <code>xstar</code> <p>List of ground-truth item IDs.</p> <code>xstar_series</code> <p>DataFrame of the ground-truth items (full catalog rows).</p> <code>xstar_simulator_view</code> <p>DataFrame of the ground-truth items as seen by the simulator (vagueified, hidden features removed).</p> <code>sec_split</code> <code>Dict[str, List[str]]</code> <p>Mapping <code>{\"search\": [...], \"experience\": [...], \"credence\": [...]}</code> partitioning feature columns by SEC category.</p> <code>initial_known_features</code> <p>Columns the simulated user knows at the start of the conversation (a subset of search features).</p> <code>available_features</code> <p>Columns in <code>xstar_simulator_view</code> that have at least one non-NA value.</p> <code>simulator_persona</code> <p>Free-text profile string passed to the user simulator (purchase history summary, demographics, etc.).</p> <code>historical_data</code> <p>Mapping from item ID to text representation of the user's past interactions.</p> <code>historical_ids</code> <p>Ordered list of item IDs in the user's history.</p> <code>historical_df</code> <p>DataFrame of historical items with an added <code>user_rating_of_5</code> column.</p> <code>baseline_queries</code> <p>List of vector-search query strings used as baselines.</p> <code>z0_baseline_query</code> <code>Optional[str]</code> <p>Vector-search query derived from <code>z0</code>.</p> <code>zstar_baseline_query</code> <code>Optional[str]</code> <p>Vector-search query derived from <code>zstar</code>.</p> <code>z0_baseline_ids</code> <code>List[str]</code> <p>Top-k item IDs retrieved using <code>z0_baseline_query</code>.</p> <code>zstar_baseline_ids</code> <code>List[str]</code> <p>Top-k item IDs retrieved using <code>zstar_baseline_query</code>.</p> <code>ustar</code> <p>Combined (column + embedding) utility function.</p> <code>column_ustar</code> <p>Column-matching-only utility function.</p> <code>embedding_ustar</code> <p>Embedding-based utility function (<code>None</code> if the vector search server is unavailable).</p> <p>Initialise a Specification.</p> <p>When <code>xstar_simulator_view</code> is provided the constructor automatically builds all z-variants via :func:<code>build_z_variants_for_spec</code> and wires up both a column-matching and an embedding-based utility function.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_name</code> <code>str</code> <p>Identifier of the parent dataset.</p> required <code>index</code> <code>str</code> <p>Position index of this spec in the dataset.</p> required <code>item_name</code> <code>Optional[str]</code> <p>Singular noun for catalog items (e.g. <code>\"book\"</code>).</p> <code>None</code> <code>z0</code> <code>Optional[str]</code> <p>Pre-built z0 string (used when <code>xstar_simulator_view</code> is <code>None</code>).</p> <code>None</code> <code>zstar</code> <code>Optional[str]</code> <p>Pre-built zstar string (used when <code>xstar_simulator_view</code> is <code>None</code>).</p> <code>None</code> <code>baseline_queries</code> <code>Optional[List[str]]</code> <p>List of query strings for vector-search baselines.</p> <code>None</code> <code>name</code> <code>Optional[str]</code> <p>Optional human-readable name for this spec.</p> <code>None</code> <code>state_files</code> <code>Optional[List[str]]</code> <p>Paths to files whose content should be captured and restored by :meth:<code>get_state</code> / :meth:<code>load_state</code>.</p> <code>None</code> <code>files_to_clean</code> <code>Optional[List[str]]</code> <p>Paths to temporary files deleted on <code>__del__</code>.</p> <code>None</code> <code>xstar</code> <code>Optional[List[str]]</code> <p>List of ground-truth item IDs.</p> <code>None</code> <code>xstar_series</code> <code>Optional[DataFrame]</code> <p>Full catalog rows of the ground-truth items.</p> <code>None</code> <code>xstar_simulator_view</code> <code>Optional[DataFrame]</code> <p>Simulator-visible rows of the ground-truth items (vagueified, hidden features removed).</p> <code>None</code> <code>sec_split</code> <code>Optional[Dict[str, List[str]]]</code> <p>SEC partition mapping <code>{\"search\": [...], \"experience\": [...], \"credence\": [...]}</code>.</p> <code>None</code> <code>initial_known_features</code> <code>Optional[List[str]]</code> <p>Columns already known to the user at the start of the conversation.</p> <code>None</code> <code>simulator_persona</code> <code>Optional[str]</code> <p>Free-text user profile passed to the simulator.</p> <code>None</code> <code>historical_data</code> <code>Optional[Dict[str, str]]</code> <p>Item-ID \u2192 text-representation mapping of past user interactions.</p> <code>None</code> <code>historical_ids</code> <code>Optional[List[str]]</code> <p>Ordered item IDs in the user's history.</p> <code>None</code> <code>historical_ratings</code> <code>Optional[List[float]]</code> <p>Per-item ratings (parallel to <code>historical_ids</code>), on a 0\u20135 scale.</p> <code>None</code> <code>catalog</code> <code>Optional[DataFrame]</code> <p>Full item catalog DataFrame (used to build <code>historical_df</code> and utility functions).</p> <code>None</code> <code>column_descriptions</code> <code>Optional[Dict[str, str]]</code> <p>Mapping from column name to human-readable feature description (shown to the simulator). May be renamed to <code>feature_descriptions</code> in a future release.</p> <code>None</code> <code>special_match_functions</code> <code>Optional[List[str]]</code> <p>Column-level match-function overrides passed to :class:<code>~coshop.data.utility.ColumnMatchingUtilityFunction</code>.</p> <code>None</code> <code>representation</code> <code>Optional[Any]</code> <p>Item representation object (unused by the constructor; stored on caller request).</p> <code>None</code> <code>z0_baseline_query</code> <code>Optional[str]</code> <p>Pre-computed vector-search query from z0.</p> <code>None</code> <code>zstar_baseline_query</code> <code>Optional[str]</code> <p>Pre-computed vector-search query from zstar.</p> <code>None</code> <code>z0_baseline_ids</code> <code>Optional[List[str]]</code> <p>Pre-retrieved top-k IDs for <code>z0_baseline_query</code>.</p> <code>None</code> <code>zstar_baseline_ids</code> <code>Optional[List[str]]</code> <p>Pre-retrieved top-k IDs for <code>zstar_baseline_query</code>.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional key-value pairs stored as instance attributes.</p> <code>{}</code>"},{"location":"api/data/datasets/#coshop.data.dataset.Specification.get_state","title":"<code>get_state()</code>","text":"<p>Serialise the current tool state for checkpointing.</p> <p>Reads the content of every path in :attr:<code>state_files</code> and returns a dict that can be passed to :meth:<code>load_state</code> to restore the same state later.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>A dict <code>{\"filenames\": [...], \"file_contents\": [...]}</code> where each</p> <code>Dict[str, Any]</code> <p>entry in <code>file_contents</code> is the file's text or <code>None</code> if the</p> <code>Dict[str, Any]</code> <p>file does not yet exist.  Returns <code>{}</code> if :attr:<code>state_files</code> is</p> <code>Dict[str, Any]</code> <p><code>None</code>.</p>"},{"location":"api/data/datasets/#coshop.data.dataset.Specification.load_state","title":"<code>load_state(state)</code>","text":"<p>Restore tool state from a previously serialised snapshot.</p> <p>Writes each file listed in <code>state[\"filenames\"]</code> with the corresponding content from <code>state[\"file_contents\"]</code>.  Entries whose content is <code>None</code> are skipped.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>Dict[str, Any]</code> <p>A dict as returned by :meth:<code>get_state</code>.</p> required"},{"location":"api/data/goodreads/","title":"Goodreads dataset","text":"<p>Goodreads book dataset for the coshop benchmark.</p> <p>Builds benchmark tasks from Goodreads user rating histories.  The held-out book(s) become xstar; earlier highly-rated books form the historical context.</p> <p>The catalog contains structured book metadata (genres, author, ratings, series info, thematic tags, etc.) merged with Goodreads review snippets. Cover images are fetched lazily from their stored URLs at render time.</p> <p>Per-version configuration (catalog/transactions paths, hidden-from-simulator columns, filterable features, special match functions, and the <code>restrict_representation_to_simulator_columns</code> default) lives in <code>v1_config.yml</code> / <code>v2_config.yml</code> alongside this module. Pass <code>version=\"v1\"</code> or <code>version=\"v2\"</code> (default <code>\"v2\"</code>) at construction to select between them.</p>"},{"location":"api/data/goodreads/#coshop.data.goodreads.data.GoodreadsDataset","title":"<code>GoodreadsDataset(indexes=None, subset_fraction=None, version='v2', **kwargs)</code>","text":"<p>               Bases: <code>Dataset</code></p> <p>Goodreads book recommendation dataset.</p> <p>Catalog: ~10k books with structured metadata (genres, author, series, rating statistics, thematic tags, tropes, etc.).  Tasks: each task is built from one user's reading/rating history.</p> <p>Assets (catalog, transactions) are downloaded from Google Drive on first instantiation if not already present in <code>data/goodreads/assets/</code>. A stock placeholder image (<code>assets/stock_image.png</code>) is used to detect and filter out uninformative cover images fetched from stored URLs.</p> <p>Initialise the Goodreads dataset.</p> <p>Parameters:</p> Name Type Description Default <code>indexes</code> <code>Optional[List[int]]</code> <p>Optional list of spec indices to pre-load.  <code>None</code> loads specs lazily.</p> <code>None</code> <code>subset_fraction</code> <code>Optional[float]</code> <p>Fraction of the catalog to retain.  xstar and selected historical items are always kept.  <code>None</code> uses the full catalog.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>~coshop.data.dataset.Dataset</code>.</p> <code>{}</code>"},{"location":"api/data/hm/","title":"H&amp;M dataset","text":"<p>H&amp;M fashion dataset for the coshop benchmark.</p> <p>Loads the H&amp;M online-shop catalog and customer transaction data.  Each benchmark task (<code>Specification</code>) is built from one customer's purchase history: the most-recent purchase(s) become the ground-truth target (xstar) and all earlier purchases form the historical context shown to the simulator.</p> <p>Per-version configuration (catalog/transactions paths, hidden-from-simulator columns, filterable features, special match functions, and the <code>restrict_representation_to_simulator_columns</code> default) lives in <code>v1_config.yml</code> / <code>v2_config.yml</code> alongside this module. Pass <code>version=\"v1\"</code> or <code>version=\"v2\"</code> (default <code>\"v2\"</code>) at construction to select between them.</p>"},{"location":"api/data/hm/#coshop.data.hm.data.HMDataset","title":"<code>HMDataset(indexes=None, subset_fraction=None, version='v2', **kwargs)</code>","text":"<p>               Bases: <code>Dataset</code></p> <p>H&amp;M fashion dataset.</p> <p>Catalog: ~105k clothing items with structured attributes (product type, colour, fit, price, fabric, etc.) and free-text product descriptions. Tasks: each task is built from one customer's transaction history.  The most-recent purchase(s) are held out as xstar; earlier purchases become the user's historical context.</p> <p>Assets are downloaded automatically from Google Drive on first instantiation if not already present in <code>data/hm/assets/</code>.</p> <p>Initialise the H&amp;M dataset.</p> <p>Parameters:</p> Name Type Description Default <code>indexes</code> <code>Optional[List[int]]</code> <p>Optional list of spec indices to pre-load.  When <code>None</code> specs are loaded lazily on first access.</p> <code>None</code> <code>subset_fraction</code> <code>float</code> <p>If set, sub-samples the catalog to this fraction of its full size while guaranteeing that all xstar and selected historical items are retained.  Useful for fast development iteration.  <code>None</code> uses the full catalog.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>~coshop.data.dataset.Dataset</code>. Common options: <code>max_xstar</code> (int), <code>max_search_features</code> (int), <code>dropout_extra_search</code> (bool).</p> <code>{}</code>"},{"location":"api/data/movielens/","title":"MovieLens dataset","text":"<p>MovieLens movie dataset for the coshop benchmark.</p> <p>Builds benchmark tasks from MovieLens user rating histories.  The held-out movie(s) a user rated most recently become xstar; earlier highly-rated movies form the historical context shown to the simulator.</p> <p>The catalog is sourced from TMDB metadata merged with MovieLens ratings. Poster images are fetched lazily from TMDB at render time.</p> <p>Per-version configuration (catalog/transactions paths, hidden-from-simulator columns, filterable features, special match functions, and the <code>restrict_representation_to_simulator_columns</code> default) lives in <code>v1_config.yml</code> / <code>v2_config.yml</code> alongside this module. Pass <code>version=\"v1\"</code> or <code>version=\"v2\"</code> (default <code>\"v2\"</code>) at construction to select between them.</p>"},{"location":"api/data/movielens/#coshop.data.movielens.data.MovieLensDataset","title":"<code>MovieLensDataset(indexes=None, subset_fraction=None, version='v2', **kwargs)</code>","text":"<p>               Bases: <code>Dataset</code></p> <p>MovieLens movie recommendation dataset.</p> <p>Catalog: ~9k movies with structured TMDB metadata (genres, year, runtime, ratings, cast tags, etc.).  Tasks: each task is built from one user's rating history; a held-out high-rated movie becomes xstar and prior high-rated movies form the historical context.</p> <p>Assets (catalog, transactions) are downloaded from Google Drive on first instantiation if not already present in <code>data/movielens/assets/</code>.</p> <p>Initialise the MovieLens dataset.</p> <p>Parameters:</p> Name Type Description Default <code>indexes</code> <code>Optional[List[int]]</code> <p>Optional list of spec indices to pre-load.  <code>None</code> loads specs lazily.</p> <code>None</code> <code>subset_fraction</code> <code>Optional[float]</code> <p>Fraction of the catalog to retain.  xstar and selected historical items are always kept.  <code>None</code> uses the full catalog.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>~coshop.data.dataset.Dataset</code>.</p> <code>{}</code>"},{"location":"api/data/representation/","title":"Representation","text":"<p>Representation classes for converting catalog rows to text formats.</p> <p>A :class:<code>Representation</code> is a stateless formatter that turns a pandas :class:<code>~pandas.Series</code> catalog row into a plain-text or markdown string suitable for passing to an LLM.  Representations are pure formatters: they accept an optional <code>columns</code> argument to restrict which columns are shown, but do not apply any corruption or noise (that logic lives in the retrieval layer).</p> Main exports <p>Representation: Abstract base class. ParagraphRepresentation: Markdown-style <code>**col (desc)**: value</code> format. representation_text_to_html: Convert paragraph representation text to HTML.</p>"},{"location":"api/data/representation/#coshop.data.representation.ParagraphRepresentation","title":"<code>ParagraphRepresentation(feature_descriptions=None, feature_order=None, **kwargs)</code>","text":"<p>               Bases: <code>Representation</code></p> <p>Markdown-style item representation.</p> <p>Formats each column as one line::</p> <pre><code>**col (description)**: value\n</code></pre> <p>where description is taken from <code>feature_descriptions</code> (with any parenthetical suffix stripped).  The <code>id</code> column is always prepended.</p> <p>Example output::</p> <pre><code>**id**: 123\n**color (Colour of the garment)**: red\n**size (Clothing size)**: large\n</code></pre> <p>Initialise a ParagraphRepresentation.</p> <p>Parameters:</p> Name Type Description Default <code>feature_descriptions</code> <code>dict</code> <p>Mapping from column name to human-readable feature description (e.g. <code>{\"color\": \"Colour of the garment (e.g. red, blue)\"}</code>).  Parenthetical suffixes are stripped automatically.  Corresponds to the <code>true_features</code> / <code>feature_descriptions</code> argument on :class:<code>~coshop.data.dataset.Dataset</code>.</p> <code>None</code> <code>feature_order</code> <code>list</code> <p>Preferred column ordering in the output.  Columns not listed here appear after those that are.  Defaults to the key order of <code>feature_descriptions</code>.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>Representation</code>.</p> <code>{}</code>"},{"location":"api/data/representation/#coshop.data.representation.ParagraphRepresentation.dump_state","title":"<code>dump_state()</code>","text":"<p>Serialize the representation state.</p>"},{"location":"api/data/representation/#coshop.data.representation.ParagraphRepresentation.row_to_str","title":"<code>row_to_str(row, columns=None)</code>","text":"<p>Format a catalog row as a markdown paragraph.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <code>Series</code> <p>A :class:<code>~pandas.Series</code> for one catalog item.</p> required <code>columns</code> <code>Optional[List[str]]</code> <p>Optional secondary column allowlist intersected with :attr:<code>~Representation.restricted_columns</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>A newline-joined string with one <code>**col (desc)**: value</code> line</p> <code>str</code> <p>per included column.</p>"},{"location":"api/data/representation/#coshop.data.representation.ParagraphRepresentation.str_to_id","title":"<code>str_to_id(text)</code>","text":"<p>Extract the item ID from a paragraph string.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>A string as produced by :meth:<code>row_to_str</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The numeric ID string (as matched by <code>**id**: &lt;digits&gt;</code>), or</p> <code>str</code> <p><code>None</code> if the pattern is not found.</p>"},{"location":"api/data/subset_catalog/","title":"Subset catalog","text":"<p>Utility for deterministically subsetting a catalog DataFrame.</p> <p>When running the benchmark with large catalogs it can be useful to work with a smaller subset for faster development iteration.  :func:<code>subset_catalog_df</code> guarantees that all ground-truth target items (<code>xstar</code>) and, depending on the chosen strategy, some historical items are always retained.</p> <p>Usage::</p> <pre><code>from coshop.data.subset_catalog import subset_catalog_df\n\nnew_catalog = subset_catalog_df(\n    catalog_df,\n    fraction=0.01,\n    xstar_ids={0: [\"id1\", \"id2\"], 1: [\"id3\"]},\n    historical_data_ids={0: [\"id4\", \"id5\"], 1: [\"id6\"]},\n)\n</code></pre>"},{"location":"api/data/subset_catalog/#coshop.data.subset_catalog.subset_catalog_df","title":"<code>subset_catalog_df(catalog, fraction, xstar_ids, historical_data_ids, strategy='xstar+prefer_historical')</code>","text":"<p>Deterministically subset a catalog DataFrame while preserving key items.</p> <p>Retains approximately <code>fraction</code> of the catalog, but always keeps:</p> <ul> <li><code>\"xstar\"</code> \u2014 all ground-truth target items.</li> <li><code>\"xstar+historical\"</code> \u2014 all target items and all historical items.</li> <li><code>\"xstar+prefer_historical\"</code> (default) \u2014 all target items plus a   greedily-selected subset of historical items (up to 3 per user), chosen   to maximise coverage across users with minimal catalog bloat.</li> </ul> <p>Remaining slots up to <code>fraction * len(catalog)</code> are filled with items from the catalog sorted by index (deterministic).</p> <p>Parameters:</p> Name Type Description Default <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame whose index contains item IDs (will be cast to <code>str</code>).</p> required <code>fraction</code> <code>float</code> <p>Target fraction of the full catalog to retain, e.g. <code>0.01</code> for 1 %.</p> required <code>xstar_ids</code> <code>dict[int, List[str]]</code> <p>Mapping from spec index to list of ground-truth item IDs for that spec.</p> required <code>historical_data_ids</code> <code>dict[int, List[str]]</code> <p>Mapping from spec index to list of historical item IDs for that spec's user.</p> required <code>strategy</code> <code>Literal['xstar', 'xstar+historical', 'xstar+prefer_historical']</code> <p>Item retention strategy; one of <code>\"xstar\"</code>, <code>\"xstar+historical\"</code>, or <code>\"xstar+prefer_historical\"</code>.</p> <code>'xstar+prefer_historical'</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>A subset of <code>catalog</code> with <code>str</code>-typed index, containing all</p> <code>DataFrame</code> <p>required items plus deterministic (index-sorted) filler up to the</p> <code>DataFrame</code> <p>target size.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the required items alone exceed the target size by more than 20 items (i.e. the requested <code>fraction</code> is too small to be feasible).</p> <code>ValueError</code> <p>If <code>strategy</code> is not one of the three supported values.</p>"},{"location":"api/data/subset_catalog/#coshop.data.subset_catalog.subset_catalog_from_transactions","title":"<code>subset_catalog_from_transactions(catalog, fraction, transactions_dir, *, test_set_size, user_suffix, history_extractor, strategy='xstar+prefer_historical')</code>","text":"<p>Subset a catalog using xstar/historical IDs read from transaction files.</p> <p>Convenience wrapper around :func:<code>subset_catalog_df</code> that gathers the required IDs directly from a dataset's transactions directory, so callers don't have to reimplement the file globbing/parsing.</p> <p>For each spec index in the test split (indices <code>[0, test_set_size)</code>), it reads <code>{idx}_items.txt</code> (first line, comma-separated) for the xstar IDs and <code>{idx}{user_suffix}</code> (JSON) for the historical IDs (via <code>history_extractor</code>).</p> <p>Parameters:</p> Name Type Description Default <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame whose index contains item IDs.</p> required <code>fraction</code> <code>float</code> <p>Target fraction of the full catalog to retain.</p> required <code>transactions_dir</code> <code>str</code> <p>Directory containing <code>{idx}_items.txt</code> and <code>{idx}{user_suffix}</code> files.</p> required <code>test_set_size</code> <code>int</code> <p>Number of held-out test specs (split boundary).</p> required <code>user_suffix</code> <code>str</code> <p>Filename suffix for the per-user JSON file, e.g. <code>\"_customer.json\"</code> or <code>\"_user.json\"</code>.</p> required <code>history_extractor</code> <code>Callable[[dict], List[str]]</code> <p>Callable mapping a parsed user-info dict to a list of historical item ID strings.</p> required <code>strategy</code> <code>Literal['xstar', 'xstar+historical', 'xstar+prefer_historical']</code> <p>Item retention strategy forwarded to :func:<code>subset_catalog_df</code>.</p> <code>'xstar+prefer_historical'</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>The subset catalog DataFrame from :func:<code>subset_catalog_df</code>.</p>"},{"location":"api/data/utility/","title":"Utility","text":"<p>Utility functions for scoring catalog items against ground-truth targets.</p> <p>A :class:<code>UtilityFunction</code> maps a list of item IDs to scalar scores in the range 0\u2013100, where 100 is a perfect match with the ground-truth target (<code>xstar</code>) and 0 is no match.  Scores are used for evaluation (NDCG) and optionally for re-ranking candidate lists.</p> <p>Available implementations:</p> <ul> <li>:class:<code>ExactMatchUtilityFunction</code> \u2014 100 iff item ID is in xstar, else 0.</li> <li>:class:<code>ColumnMatchingUtilityFunction</code> \u2014 percentage of columns that match   xstar, with pluggable per-column match functions.</li> <li>:class:<code>ServerCosinePercentileUtilityFunction</code> \u2014 cosine percentile via the   vector search API server.</li> <li>:class:<code>ColumnMatchWithServerCosinePercentileUtilityFunction</code> \u2014 weighted   combination of column matching and server cosine percentile.</li> </ul> <p>The <code>MATCH_FUNCTIONS</code> dict maps function name strings to callables and supports the following per-column override keys: <code>\"exact_match\"</code>, <code>\"jaccard_similarity\"</code>, <code>\"contains_all\"</code>, <code>\"greater_than_or_equal_to\"</code>, <code>\"less_than_or_equal_to\"</code>.</p>"},{"location":"api/data/utility/#coshop.data.utility.ColumnMatchWithServerCosinePercentileUtilityFunction","title":"<code>ColumnMatchWithServerCosinePercentileUtilityFunction(column_matching_uf, server_cosine_uf)</code>","text":"<p>               Bases: <code>UtilityFunction</code></p> <p>Combined utility function: column matching + server cosine percentile.</p> <p>Treats the server cosine-percentile score as one extra synthetic \"column\" and averages it with the column-matching score.  Formally, if <code>k = len(cols_to_compare)</code>:</p> <p>.. code-block:: none</p> <pre><code>combined = ((k * col_match_score/100) + cos_score/100) / (k + 1) * 100\n</code></pre> <p>This is the default <code>ustar</code> used by :class:<code>~coshop.data.dataset.Specification</code> when the vector search server is reachable.</p> <p>Initialise the combined utility function.</p> <p>Parameters:</p> Name Type Description Default <code>column_matching_uf</code> <code>ColumnMatchingUtilityFunction</code> <p>A fully-configured :class:<code>ColumnMatchingUtilityFunction</code> instance.</p> required <code>server_cosine_uf</code> <code>ServerCosinePercentileUtilityFunction</code> <p>A fully-configured :class:<code>ServerCosinePercentileUtilityFunction</code> instance.</p> required"},{"location":"api/data/utility/#coshop.data.utility.ColumnMatchWithServerCosinePercentileUtilityFunction.score_columns_only","title":"<code>score_columns_only(item_ids, return_metadata=False)</code>","text":"<p>Delegate to the wrapped ColumnMatchingUtilityFunction.</p>"},{"location":"api/data/utility/#coshop.data.utility.ColumnMatchWithServerCosinePercentileUtilityFunction.score_server_cosine_only","title":"<code>score_server_cosine_only(item_ids, return_metadata=False)</code>","text":"<p>Delegate to the wrapped ServerCosinePercentileUtilityFunction.</p>"},{"location":"api/data/utility/#coshop.data.utility.ColumnMatchingUtilityFunction","title":"<code>ColumnMatchingUtilityFunction(xstar, catalog, cols_to_compare=None, cols_to_explode=None, special_match_functions={})</code>","text":"<p>               Bases: <code>UtilityFunction</code></p> <p>Utility function based on the percentage of matching columns with xstar.</p> <p>For each candidate item, computes the fraction of <code>cols_to_compare</code> columns whose value matches the corresponding value in <code>xstar</code>.  The fraction is multiplied by 100 to produce a score in [0, 100].</p> <p>When <code>xstar</code> has multiple rows (multiple acceptable ground-truth items), the score is the maximum over all xstar rows.</p> <p>Per-column match logic can be customised via <code>special_match_functions</code>. Supported function names (keys of <code>MATCH_FUNCTIONS</code>):</p> <ul> <li><code>\"exact_match\"</code> (default) \u2014 <code>str(item) == str(xstar)</code>.</li> <li><code>\"jaccard_similarity\"</code> \u2014 Jaccard similarity of parsed sets.</li> <li><code>\"contains_all\"</code> \u2014 item set must be a superset of xstar set.</li> <li><code>\"greater_than_or_equal_to\"</code> \u2014 numeric <code>item &gt;= xstar</code>.</li> <li><code>\"less_than_or_equal_to\"</code> \u2014 numeric <code>item &lt;= xstar</code>.</li> </ul> <p>Initialise a ColumnMatchingUtilityFunction.</p> <p>Parameters:</p> Name Type Description Default <code>xstar</code> <code>DataFrame</code> <p>DataFrame of ground-truth items (index = item IDs).</p> required <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame (index = item IDs).</p> required <code>cols_to_compare</code> <code>List[str]</code> <p>Columns to include in the match computation. Columns missing from <code>xstar</code> are silently ignored.  Defaults to all columns of <code>xstar</code>.</p> <code>None</code> <code>cols_to_explode</code> <code>List[str]</code> <p>Columns to explode (comma-separated string \u2192 multiple rows) before comparison.  Defaults to <code>[]</code>.</p> <code>None</code> <code>special_match_functions</code> <code>Dict[str, str]</code> <p>Mapping from column name to match function name string.  Columns not listed use <code>\"exact_match\"</code>.</p> <code>{}</code>"},{"location":"api/data/utility/#coshop.data.utility.ExactMatchUtilityFunction","title":"<code>ExactMatchUtilityFunction(xstar, catalog)</code>","text":"<p>               Bases: <code>UtilityFunction</code></p> <p>Utility function based on exact item-ID match with ground-truth targets.</p> <p>Assigns 100.0 to any item whose ID is in <code>xstar</code>, and 0.0 to everything else.  Items not present in the catalog also receive 0.0.</p> <p>Initialise an ExactMatchUtilityFunction.</p> <p>Parameters:</p> Name Type Description Default <code>xstar</code> <code>DataFrame</code> <p>DataFrame of ground-truth items (index = item IDs).</p> required <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame (index = item IDs).</p> required"},{"location":"api/data/utility/#coshop.data.utility.ServerCosinePercentileUtilityFunction","title":"<code>ServerCosinePercentileUtilityFunction(xstar, dataset_name, catalog=None, api_url=None, version=None)</code>","text":"<p>               Bases: <code>UtilityFunction</code></p> <p>Utility function that scores items via cosine percentile from the vector search server.</p> <p>For each candidate item, queries the vector search API to obtain the cosine-similarity percentile of that item relative to the ground-truth xstar items.  The percentile (0\u20131) is scaled to a score in [0, 100].</p> <p>xstar items always receive a score of 100 without an API call.  Items not found in the catalog receive 0.  Requires the vector search server to be running and reachable (set <code>VECTOR_SEARCH_API_URL</code> env var or pass <code>api_url</code>).</p> <p>Initialise a ServerCosinePercentileUtilityFunction.</p> <p>Parameters:</p> Name Type Description Default <code>xstar</code> <code>List[str]</code> <p>List of ground-truth item ID strings used as reference IDs for the cosine percentile computation.</p> required <code>dataset_name</code> <code>str</code> <p>Dataset identifier forwarded to the vector search API (e.g. <code>\"hm\"</code>).</p> required <code>catalog</code> <code>Optional[DataFrame]</code> <p>Optional catalog DataFrame for item-ID validation.  When provided, unknown IDs are short-circuited to score 0 without an API call.</p> <code>None</code> <code>api_url</code> <code>Optional[str]</code> <p>Base URL of the vector search server.  Falls back to the <code>VECTOR_SEARCH_API_URL</code> environment variable.</p> <code>None</code> <code>version</code> <code>Optional[str]</code> <p>Dataset version (e.g. <code>\"v1\"</code> or <code>\"v2\"</code>) forwarded to the vector search API so the server selects the matching index.</p> <code>None</code>"},{"location":"api/data/utility/#coshop.data.utility.UtilityFunction","title":"<code>UtilityFunction</code>","text":"<p>Abstract base class for item utility scoring functions.</p> <p>A UtilityFunction assigns a scalar score in [0, 100] to each catalog item reflecting how well the item matches the user's latent preferences (xstar).</p> <p>Subclasses must implement :meth:<code>__call__</code>.</p>"},{"location":"api/evaluation/budget/","title":"Budget","text":"<p>Budget tracking for elicitation episodes.</p> <p>Tracks per-episode consumption across five dimensions (turns, runtime, tokens, questions, unique items) and signals when any configured limit is exceeded.</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker","title":"<code>BudgetTracker(budget_turns=None, budget_runtime=None, budget_tokens=None, budget_questions=None, budget_unique_items=None, policy_budget_weight=1.0, user_budget_weight=1.0)</code>","text":"<p>Tracks multiple budget dimensions and signals when any limit is exhausted.</p> <p>Each budget dimension is optional \u2014 pass <code>None</code> (the default) to impose no limit on that dimension.  Budgets are checked in <code>is_exhausted()</code>, which returns a <code>(bool, reason_str)</code> pair.</p> <p>Runtime and token costs are weighted before accumulation to allow asymmetric penalization of policy vs. user simulator costs:</p> <ul> <li><code>weighted_runtime = policy_budget_weight * policy_runtime + user_budget_weight * user_runtime</code></li> <li><code>weighted_tokens  = policy_budget_weight * policy_tokens  + user_budget_weight * user_tokens</code></li> </ul> <p>Set a weight to <code>0.0</code> to ignore that component entirely (e.g. exclude user simulator costs from the token budget).</p> <p>Attributes:</p> Name Type Description <code>budget_turns</code> <p>Maximum number of conversation turns allowed.</p> <code>budget_runtime</code> <p>Maximum weighted wall-clock seconds allowed.</p> <code>budget_tokens</code> <p>Maximum weighted token count allowed.</p> <code>budget_questions</code> <p>Maximum clarifying questions the user simulator may ask.</p> <code>budget_unique_items</code> <p>Maximum distinct catalog items the agent may surface.</p> <code>policy_budget_weight</code> <p>Multiplier applied to policy-side runtime/token costs.</p> <code>user_budget_weight</code> <p>Multiplier applied to user-simulator runtime/token costs.</p> <code>current_turns</code> <p>Turns consumed so far.</p> <code>current_runtime</code> <p>Weighted seconds consumed so far.</p> <code>current_tokens</code> <p>Weighted tokens consumed so far (policy + user).</p> <code>current_policy_tokens</code> <p>Raw policy token count (unweighted).</p> <code>current_user_tokens</code> <p>Raw user-simulator token count (unweighted).</p> <code>current_questions</code> <p>Clarifying questions asked so far.</p> <code>current_unique_items</code> <p>Distinct catalog items surfaced so far.</p> <p>Initialize a BudgetTracker.</p> <p>Parameters:</p> Name Type Description Default <code>budget_turns</code> <code>Optional[int]</code> <p>Maximum conversation turns before exhaustion.  <code>None</code> means no turn limit.</p> <code>None</code> <code>budget_runtime</code> <code>Optional[float]</code> <p>Maximum weighted wall-clock seconds before exhaustion. <code>None</code> means no runtime limit.</p> <code>None</code> <code>budget_tokens</code> <code>Optional[int]</code> <p>Maximum weighted tokens before exhaustion.  <code>None</code> means no token limit.</p> <code>None</code> <code>budget_questions</code> <code>Optional[int]</code> <p>Maximum clarifying questions the user simulator may ask.  Checked externally via <code>get_remaining_questions()</code>; does not trigger <code>is_exhausted()</code>.  <code>None</code> means no limit.</p> <code>None</code> <code>budget_unique_items</code> <code>Optional[int]</code> <p>Maximum distinct catalog items the agent may surface.  Checked externally via <code>get_remaining_unique_items()</code>; does not trigger <code>is_exhausted()</code>.  <code>None</code> means no limit.</p> <code>None</code> <code>policy_budget_weight</code> <code>float</code> <p>Multiplier for policy-side runtime/token costs. Defaults to <code>1.0</code>.</p> <code>1.0</code> <code>user_budget_weight</code> <code>float</code> <p>Multiplier for user-simulator runtime/token costs. Defaults to <code>1.0</code>.</p> <code>1.0</code>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.add_questions","title":"<code>add_questions(count)</code>","text":"<p>Add to the clarifying-question counter.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>Number of questions asked in the current turn.</p> required"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.add_runtime","title":"<code>add_runtime(policy_runtime, user_runtime)</code>","text":"<p>Accumulate weighted wall-clock runtime.</p> <p>Parameters:</p> Name Type Description Default <code>policy_runtime</code> <code>float</code> <p>Seconds spent in the policy (agent) this turn.</p> required <code>user_runtime</code> <code>float</code> <p>Seconds spent in the user simulator this turn.</p> required"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.add_tokens","title":"<code>add_tokens(policy_tokens, user_tokens)</code>","text":"<p>Accumulate weighted token usage.</p> <p>Internally tracks raw policy and user token counts separately, then recomputes <code>current_tokens</code> as: <code>policy_budget_weight * total_policy_tokens + user_budget_weight * total_user_tokens</code>.</p> <p>Parameters:</p> Name Type Description Default <code>policy_tokens</code> <code>int</code> <p>Tokens used by the policy this turn.</p> required <code>user_tokens</code> <code>int</code> <p>Tokens used by the user simulator this turn.</p> required"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.add_turn","title":"<code>add_turn()</code>","text":"<p>Increment the turn counter by one.</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.add_unique_items","title":"<code>add_unique_items(count)</code>","text":"<p>Add to the unique-items-surfaced counter.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>Number of new distinct catalog items shown in the current turn.</p> required"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.get_metrics","title":"<code>get_metrics()</code>","text":"<p>Return a snapshot of current usage and remaining budget for all dimensions.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>A dict with keys <code>\"turns\"</code>, <code>\"runtime\"</code>, <code>\"tokens\"</code>,</p> <code>Dict[str, Any]</code> <p><code>\"questions\"</code>, and <code>\"unique_items\"</code>.  Each value is a dict with</p> <code>Dict[str, Any]</code> <p><code>\"current\"</code>, <code>\"budget\"</code> (limit or <code>None</code>), and <code>\"remaining\"</code></p> <code>Dict[str, Any]</code> <p>(remaining capacity or <code>None</code> if unlimited) sub-keys.  The</p> <code>Dict[str, Any]</code> <p><code>\"tokens\"</code> entry additionally includes <code>\"policy_tokens\"</code> and</p> <code>Dict[str, Any]</code> <p><code>\"user_tokens\"</code> (raw unweighted counts) plus <code>\"policy_breakdown\"</code></p> <code>Dict[str, Any]</code> <p>and <code>\"user_breakdown\"</code> (per-bucket input/input_cached/output/</p> <code>Dict[str, Any]</code> <p>reasoning splits, populated via <code>set_*_token_breakdown</code>).</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.get_remaining_questions","title":"<code>get_remaining_questions()</code>","text":"<p>Return the number of clarifying questions still permitted.</p> <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Remaining question budget (\u2265 0), or <code>None</code> if no limit is set.</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.get_remaining_unique_items","title":"<code>get_remaining_unique_items()</code>","text":"<p>Return the number of additional unique items that may still be surfaced.</p> <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Remaining unique-items budget (\u2265 0), or <code>None</code> if no limit is set.</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.is_exhausted","title":"<code>is_exhausted()</code>","text":"<p>Check whether any hard budget limit has been reached.</p> <p>Checks turns, runtime, and tokens in that order.  Questions and unique-items budgets are soft limits surfaced via <code>get_remaining_questions()</code> / <code>get_remaining_unique_items()</code> and are not checked here.</p> <p>Returns:</p> Type Description <code>bool</code> <p>A <code>(exhausted, reason)</code> tuple where <code>exhausted</code> is <code>True</code> if</p> <code>str</code> <p>any limit is reached and <code>reason</code> is one of</p> <code>Tuple[bool, str]</code> <p><code>\"turns_exhausted\"</code>, <code>\"runtime_exhausted\"</code>,</p> <code>Tuple[bool, str]</code> <p><code>\"tokens_exhausted\"</code>, or <code>\"\"</code> (not exhausted).</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.set_policy_token_breakdown","title":"<code>set_policy_token_breakdown(breakdown)</code>","text":"<p>Overwrite policy token breakdown with the agent's cumulative counts.</p>"},{"location":"api/evaluation/budget/#coshop.evaluation.budget.BudgetTracker.set_user_token_breakdown","title":"<code>set_user_token_breakdown(breakdown)</code>","text":"<p>Overwrite user token breakdown with the simulator's cumulative counts.</p>"},{"location":"api/evaluation/metrics/","title":"Metrics","text":"<p>Evaluation metrics for the coshop benchmark.</p> <p>Given a ranked list of item IDs produced by a policy, these functions compute utility-based and exact-match metrics against the ground-truth xstar.</p> Main exports <p>compute_ndcg_at_k: Standard NDCG@k over a relevance score list. compute_evaluation_metrics: Full evaluation pipeline returning an     :class:<code>~coshop.utils.custom_types.Evaluation</code> dataclass.</p>"},{"location":"api/evaluation/metrics/#coshop.evaluation.metrics.compute_evaluation_metrics","title":"<code>compute_evaluation_metrics(*, spec, ranked_item_ids, catalog, execution_max_per_retrieval, execution_max_queries=None, execution_global_max=None, k, seen_item_ids=None, compute_column_ustar=True, compute_embedding_ustar=True, cache_dir=None)</code>","text":"<p>Compute evaluation metrics for one elicitation episode.</p> <p>Scores all catalog items once (with optional caching), then computes seen metrics (items the policy retrieved during the conversation) and predicted metrics (the final ranked list returned by the policy).</p> <p>Predicted metrics are computed at both <code>@1</code> and <code>@k</code>:</p> <ul> <li><code>max_ustar_at_k</code> / <code>avg_ustar_at_k</code> \u2014 max / mean combined utility   score in the top-k.</li> <li><code>ndcg_ustar_at_k</code> \u2014 NDCG@k using combined utility as relevance.</li> <li><code>recall_at_k</code> \u2014 1 if any xstar item appears in the top-k, else 0.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>spec</code> <p>:class:<code>~coshop.data.dataset.Specification</code> providing the utility functions and xstar reference.</p> required <code>ranked_item_ids</code> <code>List[str]</code> <p>Ordered list of item IDs predicted by the policy (position 0 = most-preferred).  May contain duplicates; they are deduplicated while preserving order.</p> required <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame (index = item IDs).</p> required <code>execution_max_per_retrieval</code> <code>Optional[int]</code> <p>Maximum items returned per retrieval call (stored in the :class:<code>~coshop.utils.custom_types.Evaluation</code> metadata, formerly called <code>m</code>).</p> required <code>execution_max_queries</code> <code>Optional[int]</code> <p>Maximum number of retrieval queries allowed during the episode (stored in metadata).</p> <code>None</code> <code>execution_global_max</code> <code>Optional[int]</code> <p>Maximum total items across all queries (stored in metadata).</p> <code>None</code> <code>k</code> <code>int</code> <p>Number of top positions used for <code>@k</code> metrics.</p> required <code>seen_item_ids</code> <code>Optional[List[str]]</code> <p>Item IDs that the policy retrieved at any point during the conversation (not just the final ranked list).  Defaults to <code>[]</code>.</p> <code>None</code> <code>compute_column_ustar</code> <code>bool</code> <p>Whether to compute column-matching utility scores.  Defaults to <code>True</code>.</p> <code>True</code> <code>compute_embedding_ustar</code> <code>bool</code> <p>Whether to compute embedding-based utility scores (requires vector search server).  Defaults to <code>True</code>.</p> <code>True</code> <code>cache_dir</code> <code>Optional[str]</code> <p>Optional directory for the :class:<code>~coshop.utils.caches.ScoringCache</code>.  <code>None</code> disables caching.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>An</code> <code>Evaluation</code> <p>class:<code>~coshop.utils.custom_types.Evaluation</code> dataclass</p> <code>Evaluation</code> <p>containing both seen and predicted sub-evaluations.</p>"},{"location":"api/evaluation/metrics/#coshop.evaluation.metrics.compute_ndcg_at_k","title":"<code>compute_ndcg_at_k(relevance_scores, k)</code>","text":"<p>Compute Normalised Discounted Cumulative Gain at k (NDCG@k).</p> <p>Positions are 1-indexed; the discount at position <code>i</code> is <code>1 / log2(i + 1)</code>.  The ideal DCG is computed from the same <code>relevance_scores</code> list sorted in descending order (not from a separate oracle list).</p> <p>Parameters:</p> Name Type Description Default <code>relevance_scores</code> <code>list</code> <p>Relevance scores in the predicted ranking order (e.g. <code>ustar</code> values for items ranked by the policy, with remaining catalog items appended at the end).</p> required <code>k</code> <code>int</code> <p>Number of top positions to consider.</p> required <p>Returns:</p> Type Description <code>float</code> <p>NDCG@k as a float in [0, 1].  Returns <code>0.0</code> when the list is empty,</p> <code>float</code> <p><code>k</code> is zero, or the ideal DCG is zero.</p>"},{"location":"api/tools/catalog_retrieval/","title":"Catalog retrieval","text":"<p>Catalog retrieval tools for shopping-assistant policies.</p> <p>Provides two LangChain tool factories:</p> <ul> <li>:func:<code>get_hard_filter_retrieval_tool</code> \u2014 exposes a <code>search_web(query, max_items, filters)</code>   tool that accepts optional column-level hard filters before running semantic search.</li> <li>:func:<code>get_retrieval_tool</code> \u2014 simpler <code>search_web(query, max_items)</code> variant with no   explicit filter argument.</li> </ul> <p>Both tools enforce optional per-call and global item-count budgets at runtime.</p>"},{"location":"api/tools/catalog_retrieval/#coshop.tools.catalog_retrieval.get_hard_filter_retrieval_tool","title":"<code>get_hard_filter_retrieval_tool(retrieval_name, catalog, filterable_features, max_items_limit=None, execution_max_queries=None, execution_global_max=None, show_col_names=False, **retrieval_fn_kwargs)</code>","text":"<p>Return a LangChain tool that searches the catalog with optional hard column filters.</p> <p>Unlike :func:<code>get_retrieval_tool</code>, the returned <code>search_web</code> tool accepts an optional <code>filters</code> argument so the agent can pin exact column values before the semantic/BM25 search runs.  Filters are applied by computing an <code>allowed_ids</code> set and passing it to the underlying retrieval function.</p> <p>The inner <code>search_web</code> tool signature is::</p> <pre><code>search_web(query: str, max_items: int, filters: Optional[Dict[str, List[Any]]] = None)\n</code></pre> <p>Filter semantics:</p> <ul> <li>Keys are catalog column names (must be in <code>filterable_features</code>).</li> <li>Values are lists of exact allowed values (string-coerced before comparison).</li> <li>Include <code>\"NA\"</code> in the list to match rows where the column is null/missing.</li> <li>No negation, inequality, or partial matching is supported.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>retrieval_name</code> <code>str</code> <p>Retrieval backend to use.  Forwarded to :func:<code>~coshop.tools.retrieval.retrieval.get_retrieval_fn</code> along with <code>catalog</code>, <code>eval_expression_columns=filterable_features</code>, and any extra <code>**retrieval_fn_kwargs</code>.</p> required <code>catalog</code> <code>DataFrame</code> <p>Full catalog DataFrame.  Used to derive <code>allowed_ids</code> from <code>filters</code> and to build per-column example values when <code>show_col_names=True</code>.</p> required <code>filterable_features</code> <code>List[str]</code> <p>Column names the agent may filter on.  Attempting to filter on any other column raises <code>ValueError</code> at call time.</p> required <code>max_items_limit</code> <code>Optional[int]</code> <p>If set, the tool raises <code>ValueError</code> when the agent requests more than this many items in a single call.</p> <code>None</code> <code>execution_max_queries</code> <code>Optional[int]</code> <p>If set, the tool raises <code>RuntimeError</code> after this many calls.</p> <code>None</code> <code>execution_global_max</code> <code>Optional[int]</code> <p>If set, the tool raises <code>RuntimeError</code> once the cumulative <code>max_items</code> requested across all calls exceeds this limit.</p> <code>None</code> <code>show_col_names</code> <code>bool</code> <p>When <code>True</code>, the tool description lists each filterable column with up to 10 example values.  When <code>False</code> (default), a shorter generic description is shown.</p> <code>False</code> <code>**retrieval_fn_kwargs</code> <p>Additional keyword arguments forwarded to :func:<code>~coshop.tools.retrieval.retrieval.get_retrieval_fn</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A LangChain <code>Tool</code> wrapping <code>search_web</code>.</p>"},{"location":"api/tools/catalog_retrieval/#coshop.tools.catalog_retrieval.get_retrieval_tool","title":"<code>get_retrieval_tool(retrieval_name, max_items_limit=None, execution_max_queries=None, execution_global_max=None, **retrieval_fn_kwargs)</code>","text":"<p>Return a simple LangChain tool that searches the catalog without explicit filters.</p> <p>The returned <code>search_web</code> tool signature is::</p> <pre><code>search_web(query: str, max_items: int)\n</code></pre> <p>Use :func:<code>get_hard_filter_retrieval_tool</code> instead if the agent should be able to restrict results to exact column values.</p> <p>Parameters:</p> Name Type Description Default <code>retrieval_name</code> <code>str</code> <p>Retrieval backend to use.  Forwarded to :func:<code>~coshop.tools.retrieval.retrieval.get_retrieval_fn</code> along with any extra <code>**retrieval_fn_kwargs</code>.</p> required <code>max_items_limit</code> <code>Optional[int]</code> <p>If set, raises <code>ValueError</code> when the agent requests more than this many items in a single call.</p> <code>None</code> <code>execution_max_queries</code> <code>Optional[int]</code> <p>If set, raises <code>RuntimeError</code> after this many calls.</p> <code>None</code> <code>execution_global_max</code> <code>Optional[int]</code> <p>If set, raises <code>RuntimeError</code> once cumulative <code>max_items</code> requested exceeds this limit.</p> <code>None</code> <code>**retrieval_fn_kwargs</code> <p>Additional keyword arguments forwarded to :func:<code>~coshop.tools.retrieval.retrieval.get_retrieval_fn</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A LangChain <code>Tool</code> wrapping <code>search_web</code>.</p>"},{"location":"api/tools/history_search/","title":"History search","text":"<p>History search tool for policies that can search the user's purchase/rating/review history.</p> <p>Uses the existing vector_search API with allowed_ids to restrict results to items in the user's history, then augments each result with the historical_data value (user-specific info: ratings, reviews, \"I purchased this\", etc.).</p>"},{"location":"api/tools/history_search/#coshop.tools.history_search.get_history_search_tool","title":"<code>get_history_search_tool(historical_data, dataset_name, version=None, api_url=None, max_items_limit=None, threshold=None, catalog=None, filterable_features=None)</code>","text":"<p>Get a tool that allows the policy to search the user's purchase/rating/review history.</p> <p>Parameters:</p> Name Type Description Default <code>historical_data</code> <code>Dict[str, str]</code> <p>Dict mapping item IDs to user-specific strings (e.g., ratings, reviews).</p> required <code>dataset_name</code> <code>str</code> <p>Dataset name for the vector_search API.</p> required <code>api_url</code> <code>Optional[str]</code> <p>Vector search API URL. If None, uses VECTOR_SEARCH_API_URL env var.</p> <code>None</code> <code>max_items_limit</code> <code>Optional[int]</code> <p>If provided, caps max_items per call.</p> <code>None</code> <code>threshold</code> <code>Optional[float]</code> <p>Optional similarity threshold for vector search.</p> <code>None</code> <code>catalog</code> <code>Optional[DataFrame]</code> <p>Optional DataFrame of catalog items (used for column filtering). Ideally the historical subset only (spec.historical_df).</p> <code>None</code> <code>filterable_features</code> <code>Optional[List[str]]</code> <p>Optional list of column names the agent may filter on. If None, the filters argument of the tool is accepted but ignored.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tool</code> <p>A LangChain Tool for search_user_purchase_history(query, max_items, filters).</p>"},{"location":"api/tools/reflect/","title":"Reflect","text":"<p>Chain-of-thought scratchpad tool for shopping-assistant policies.</p> <p>Provides :func:<code>get_reflect_tool</code>, which returns a no-op tool that echoes the agent's reasoning back to it.  The tool is meant to make the agent's internal reasoning visible in evaluation traces without affecting the user-facing conversation.</p>"},{"location":"api/tools/reflect/#coshop.tools.reflect.get_reflect_tool","title":"<code>get_reflect_tool()</code>","text":"<p>Return a LangChain tool for chain-of-thought reflection.</p> <p>The returned <code>reflect</code> tool accepts a free-text <code>thought</code> string and echoes it back unchanged.  It is intended as a scratchpad: calling it does not affect the user-facing conversation, but the call and its argument are recorded in the evaluation trace, making the agent's reasoning inspectable.</p> <p>Returns:</p> Type Description <p>A LangChain <code>Tool</code> wrapping <code>reflect(thought: str) -&gt; str</code>.</p>"},{"location":"api/tools/retrieval/","title":"Retrieval backends","text":"<p>Core retrieval abstractions and the LLM-based eval-expression prefilter.</p> <p>Main exports:</p> <ul> <li>:class:<code>RetrievalFunction</code> \u2014 abstract base; <code>__call__</code> returns a two-column   DataFrame (<code>id</code>, <code>text</code>).</li> <li>:class:<code>DFRetrievalFunction</code> \u2014 concrete base backed by a pandas DataFrame catalog;   adds the LLM-generated eval-expression prefilter pipeline.</li> <li>:func:<code>get_retrieval_fn</code> \u2014 factory that instantiates <code>BM25</code> or <code>VectorSearch</code>   by name.</li> </ul> <p>BM25 retrieval backend for catalog search.</p> <p>Uses the Okapi BM25 algorithm (via the <code>rank_bm25</code> library) to rank items in a pandas DataFrame catalog against a natural-language query.  Text is normalized (unicode NFKD, lowercase, whitespace collapse, optional <code>ftfy</code> encoding repair) before indexing and at query time.</p> <p>Vector-search retrieval backend backed by a remote API server.</p> <p>Delegates embedding computation and nearest-neighbour lookup to a running :mod:<code>coshop.tools.vector_search_server</code> instance via :class:<code>~coshop.tools.vector_search_api_client.VectorSearchAPIClient</code>. Inherits the eval-expression prefilter pipeline from :class:<code>~coshop.tools.retrieval.retrieval.DFRetrievalFunction</code>.</p>"},{"location":"api/tools/retrieval/#coshop.tools.retrieval.retrieval.DFRetrievalFunction","title":"<code>DFRetrievalFunction(catalog, representation, retrieval_representation=None, output_ordering='relevance', max_text_len=None, popularity_df=None, prefilter=True, num_expression_ordering_trials=5, eval_expression_model_name='gpt-5-nano', temperature=0.0, eval_expression_columns=None, verbose=False, **eval_expression_model_kwargs)</code>","text":"<p>               Bases: <code>RetrievalFunction</code></p> <p>DataFrame-backed retrieval with LLM-generated eval-expression prefiltering.</p> <p>This class wraps a pandas DataFrame catalog and adds an optional eval-expression prefilter pipeline that runs before the semantic search:</p> <ol> <li>An LLM is prompted with the query and a list of filterable column names    plus example values.  It returns a JSON list of simple <code>pandas.DataFrame.eval</code>    boolean expressions (one simple clause per item, e.g.    <code>\"genres.str.contains('Comedy')\"</code>.</li> <li>Expressions are applied greedily in order: each expression is applied    only if it would leave at least one row; otherwise it is skipped.</li> <li>The resulting filtered <code>allowed_ids</code> are passed to :meth:<code>_retrieve</code>.</li> <li>If the filtered results are fewer than <code>m</code>, they are supplemented with    unrestricted retrieval results (deduplication preserves filtered results first).</li> </ol> <p>This two-stage approach can dramatically narrow the search space before BM25 or vector similarity scoring, improving precision for highly specific queries.</p> <p>Parameters:</p> Name Type Description Default <code>catalog</code> <code>DataFrame</code> <p>Full item catalog.  The DataFrame index is used as item IDs.</p> required <code>representation</code> <code>Representation</code> <p>:class:<code>~coshop.data.representation.Representation</code> used to render each catalog row into the <code>text</code> field returned to callers.</p> required <code>retrieval_representation</code> <code>Optional[Representation]</code> <p>Representation used internally during retrieval (e.g. BM25 index construction).  If <code>None</code>, <code>representation</code> is used for both retrieval and output.</p> <code>None</code> <code>output_ordering</code> <code>Literal['random', 'id', 'relevance', 'popularity']</code> <p>Result ordering.  See :class:<code>RetrievalFunction</code>. <code>\"popularity\"</code> requires <code>popularity_df</code>.</p> <code>'relevance'</code> <code>max_text_len</code> <code>Optional[int]</code> <p>Per-item text truncation length.  <code>None</code> disables truncation.</p> <code>None</code> <code>popularity_df</code> <code>Optional[DataFrame]</code> <p>DataFrame indexed by item ID with a <code>\"popularity\"</code> column.  Required when <code>output_ordering=\"popularity\"</code>.</p> <code>None</code> <code>prefilter</code> <code>bool</code> <p>When <code>True</code> (default), run the LLM eval-expression pipeline before retrieval.  When <code>False</code>, pass the query directly to :meth:<code>_retrieve</code> with no prefiltering.</p> <code>True</code> <code>num_expression_ordering_trials</code> <code>int</code> <p>Stored for backwards compatibility but not currently used; eval expressions are applied greedily in the order the LLM generated them (see :meth:<code>_apply_eval_expressions_greedy</code>).  Defaults to <code>5</code>.</p> <code>5</code> <code>eval_expression_model_name</code> <code>str</code> <p>LLM model name used to generate eval expressions.  Defaults to <code>\"gpt-5-nano\"</code>.</p> <code>'gpt-5-nano'</code> <code>temperature</code> <code>float</code> <p>Sampling temperature for the eval-expression LLM. Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>eval_expression_columns</code> <code>Optional[List[str]]</code> <p>Subset of catalog columns the LLM may filter on.  If <code>None</code>, no eval-expression columns are available and prefiltering degrades to unrestricted retrieval.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If <code>True</code>, print eval-expression decisions to stdout.</p> <code>False</code> <code>**eval_expression_model_kwargs</code> <p>Extra kwargs forwarded to the OpenAI client (e.g. <code>base_url</code>, <code>api_key</code>, <code>model_provider</code>, <code>vllm_api_url</code>).  <code>model_provider=\"vllm\"</code> together with <code>vllm_api_url</code> routes requests to a local vLLM server.</p> <code>{}</code>"},{"location":"api/tools/retrieval/#coshop.tools.retrieval.retrieval.RetrievalFunction","title":"<code>RetrievalFunction(output_ordering='relevance', max_text_len=2000)</code>","text":"<p>Abstract base class for catalog retrieval functions.</p> <p>Subclasses implement :meth:<code>_retrieve</code>; callers use :meth:<code>__call__</code>, which enforces the output contract (columns <code>[\"id\", \"text\"]</code>, at most <code>m</code> rows, optional text truncation, and result ordering).</p> <p>Attributes:</p> Name Type Description <code>OUTPUT_COLUMNS</code> <p>Always <code>[\"id\", \"text\"]</code>.</p> <code>output_ordering</code> <p>How results are ordered before returning.  One of <code>\"random\"</code>, <code>\"id\"</code>, <code>\"relevance\"</code> (default \u2014 subclass ordering is preserved), or <code>\"popularity\"</code> (requires a <code>popularity_df</code>; only supported in :class:<code>DFRetrievalFunction</code>).</p> <code>max_text_len</code> <p>If set, each <code>text</code> value is truncated to this many characters before returning.</p> <p>Parameters:</p> Name Type Description Default <code>output_ordering</code> <code>Literal['random', 'id', 'relevance', 'popularity']</code> <p>Result ordering applied after retrieval.  <code>\"random\"</code> shuffles results; <code>\"id\"</code> sorts by item ID; <code>\"relevance\"</code> preserves the order returned by :meth:<code>_retrieve</code>; <code>\"popularity\"</code> sorts by the <code>popularity</code> column in <code>popularity_df</code> (only available in :class:<code>DFRetrievalFunction</code>).</p> <code>'relevance'</code> <code>max_text_len</code> <code>Optional[int]</code> <p>Truncate each <code>text</code> value to this many characters. <code>None</code> disables truncation.  Defaults to <code>2000</code>.</p> <code>2000</code>"},{"location":"api/tools/retrieval/#coshop.tools.retrieval.retrieval.get_retrieval_fn","title":"<code>get_retrieval_fn(retrieval_name, **kwargs)</code>","text":"<p>Instantiate a retrieval backend by name.</p> <p>Parameters:</p> Name Type Description Default <code>retrieval_name</code> <code>str</code> <p>Backend to use.  One of <code>\"BM25\"</code> or <code>\"VectorSearch\"</code>.</p> required <code>**kwargs</code> <p>Keyword arguments forwarded to the selected class constructor.</p> <code>{}</code> <p>Returns:</p> Type Description <p>An instance of :class:<code>BM25</code> or :class:<code>VectorSearch</code>.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If <code>retrieval_name</code> is not a recognised backend.</p>"},{"location":"api/tools/retrieval/#coshop.tools.retrieval.bm25.BM25","title":"<code>BM25(catalog, representation, threshold=None, **kwargs)</code>","text":"<p>               Bases: <code>DFRetrievalFunction</code></p> <p>Catalog retrieval using the Okapi BM25 ranking algorithm.</p> <p>An inverted-token index is built from the catalog at construction time. All query and document text is normalized and tokenized with :meth:<code>_normalize_string</code> and :meth:<code>_tokenize</code> before indexing or scoring.</p> <p>Inherits the eval-expression prefilter pipeline from :class:<code>~coshop.tools.retrieval.retrieval.DFRetrievalFunction</code>.</p> <p>Parameters:</p> Name Type Description Default <code>catalog</code> <code>DataFrame</code> <p>Full item catalog DataFrame.  The index is used as item IDs.</p> required <code>representation</code> <code>Representation</code> <p>Converts each catalog row to the text string that is indexed and returned to callers.</p> required <code>threshold</code> <code>float</code> <p>Minimum BM25 score required for a result to be returned. Items scoring below this value are dropped.  <code>None</code> (default) disables score filtering.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>~coshop.tools.retrieval.retrieval.DFRetrievalFunction</code>.</p> <code>{}</code>"},{"location":"api/tools/retrieval/#coshop.tools.retrieval.vector_search.VectorSearch","title":"<code>VectorSearch(catalog, representation, vector_search_api_url, dataset_name=None, version=None, threshold=None, output_ordering='relevance', **kwargs)</code>","text":"<p>               Bases: <code>DFRetrievalFunction</code></p> <p>Catalog retrieval via a remote vector-search API server.</p> <p>At query time, the query string is sent to the server along with an <code>allowed_ids</code> whitelist derived from the local catalog (and any eval-expression prefilter).  The server returns ranked item IDs and similarity scores; the local catalog text is used if the server does not return item text.</p> <p>Parameters:</p> Name Type Description Default <code>catalog</code> <code>DataFrame</code> <p>Catalog DataFrame.</p> required <code>representation</code> <code>Representation</code> <p>Representation used to convert rows to text.</p> required <code>vector_search_api_url</code> <code>str</code> <p>Base URL of the vector search API server.</p> required <code>dataset_name</code> <code>Optional[str]</code> <p>Dataset name sent to the server so it loads the correct catalog. Required; a <code>ValueError</code> is raised if not provided.</p> <code>None</code> <code>version</code> <code>Optional[str]</code> <p>Optional dataset version forwarded to the server.</p> <code>None</code> <code>threshold</code> <code>Optional[float]</code> <p>Optional similarity threshold; results below it are dropped.</p> <code>None</code> <code>output_ordering</code> <code>str</code> <p>\"relevance\" (default), \"random\", or \"id\".</p> <code>'relevance'</code>"},{"location":"api/tools/vector_search/","title":"Vector search server","text":"<p>VectorSearch API Client</p> <p>Used by the local <code>query/vector_search.py</code> QueryFunction to delegate retrieval to a shared server that holds the embedding model on GPU.</p> <p>Also supports embedding operations (encode/encode_query) as a drop-in replacement for EmbeddingAPIClient.</p> Env var <p>VECTOR_SEARCH_API_URL=http://localhost:8001</p> <p>CoShop vector search API server (HNSW).</p> <p>Builds embedding indexes at startup for all CoShop datasets, then serves:   POST /vector_search      - HNSW nearest-neighbour search   POST /encode             - Encode documents into embeddings   POST /encode_query       - Encode queries into embeddings   POST /cosine_distance    - Exact cosine distance between ids   POST /cosine_percentile  - Percentile rank of an item's similarity score   GET  /health             - Health check</p> <p>Usage (development):     python -m coshop.tools.vector_search_server \\         --embedding_api_url http://localhost:8000 \\         --embedding_model my-embed-model</p> <p>Usage (production via gunicorn):     EMBEDDING_API_URL=http://localhost:8000 EMBEDDING_MODEL=my-embed-model gunicorn \\         --workers 1 --bind 0.0.0.0:3004 \\         \"coshop.tools.vector_search_server:create_app()\"</p> <p>The model id (EMBEDDING_MODEL / --embedding_model) is required and must match the name the backend serves the model under (e.g. vLLM's --served-model-name, or \"text-embedding-3-small\" for OpenAI).</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient","title":"<code>VectorSearchAPIClient(api_url=None, timeout_s=120.0)</code>","text":"<p>HTTP client for the CoShop vector-search API server.</p> <p>Wraps all endpoints of :mod:<code>coshop.tools.vector_search_server</code> and also implements the <code>encode</code> / <code>encode_query</code> interface so it can be used as a drop-in replacement for any embedding model wrapper.</p> <p>The client pings <code>/health</code> during <code>__init__</code> to fail fast if the server is unreachable.</p> <p>Attributes:</p> Name Type Description <code>api_url</code> <p>Base URL of the running server (trailing slash stripped).</p> <code>timeout_s</code> <p>Request timeout in seconds (default 120).</p> <code>model_name</code> <p>Embedding model name reported by the server <code>/health</code> endpoint; useful for cache-key generation.</p> <p>Parameters:</p> Name Type Description Default <code>api_url</code> <code>Optional[str]</code> <p>Base URL of the vector search server (e.g. <code>\"http://localhost:3004\"</code>).  Falls back to the <code>VECTOR_SEARCH_API_URL</code> environment variable if not provided.</p> <code>None</code> <code>timeout_s</code> <code>float</code> <p>Per-request HTTP timeout in seconds.  Defaults to <code>120.0</code>.</p> <code>120.0</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither <code>api_url</code> nor <code>VECTOR_SEARCH_API_URL</code> is set.</p> <code>HTTPError</code> <p>If the health check fails.</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient.cosine_distance","title":"<code>cosine_distance(*, dataset, query_id, reference_ids, version=None, dataset_kwargs=None)</code>","text":"<p>Compute minimum cosine distance between query_ids and any reference_id. Supports batching for efficient processing of many query_ids.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>str</code> <p>Dataset name</p> required <code>query_id</code> <code>List[str]</code> <p>List of query item IDs</p> required <code>reference_ids</code> <code>List[str]</code> <p>List of reference item IDs</p> required <code>dataset_kwargs</code> <code>Optional[Dict[str, Any]]</code> <p>Optional dataset configuration</p> <code>None</code> <p>Returns:</p> Type Description <code>List[float]</code> <p>List of minimum cosine distances (one per query_id)</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient.cosine_percentile","title":"<code>cosine_percentile(*, dataset, query_id, reference_ids, version=None, dataset_kwargs=None)</code>","text":"<p>Compute percentile of query_ids' score compared to all items in the catalog. Supports batching for efficient processing of many query_ids.</p> <p>Score for each item v is: min_{x in reference_ids} cosine_distance(v, x) Returns the percentile of score(query_id) compared to scores of all catalog items.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>str</code> <p>Dataset name</p> required <code>query_id</code> <code>List[str]</code> <p>List of query item IDs</p> required <code>reference_ids</code> <code>List[str]</code> <p>List of reference item IDs</p> required <code>dataset_kwargs</code> <code>Optional[Dict[str, Any]]</code> <p>Optional dataset configuration</p> <code>None</code> <p>Returns:</p> Type Description <code>List[float]</code> <p>List of percentiles (0-1, one per query_id) of query_ids' scores</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient.encode","title":"<code>encode(texts, show_progress_bar=False, **kwargs)</code>","text":"<p>Encode texts into embeddings (for documents).</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>Union[str, List[str]]</code> <p>Text(s) to encode</p> required <code>show_progress_bar</code> <code>bool</code> <p>Whether to show progress bar (ignored for API)</p> <code>False</code> <code>**kwargs</code> <p>Additional arguments (ignored for API client)</p> <code>{}</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>numpy array of embeddings</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient.encode_query","title":"<code>encode_query(texts, show_progress_bar=False, **kwargs)</code>","text":"<p>Encode queries into embeddings (for queries).</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>Union[str, List[str]]</code> <p>Text(s) to encode as queries</p> required <code>show_progress_bar</code> <code>bool</code> <p>Whether to show progress bar (ignored for API)</p> <code>False</code> <code>**kwargs</code> <p>Additional arguments (ignored for API client)</p> <code>{}</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>numpy array of embeddings</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_api_client.VectorSearchAPIClient.vector_search","title":"<code>vector_search(*, dataset, q, m, version=None, dataset_kwargs=None, threshold=None, corrupt_representations=False, corruption_seed=None, allowed_ids=None)</code>","text":"<p>Run a nearest-neighbour vector search against the server.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>str</code> <p>Dataset name registered on the server (e.g. <code>\"hm\"</code>).</p> required <code>q</code> <code>str</code> <p>Natural-language query string.  An empty string returns items in catalog order.</p> required <code>m</code> <code>Optional[int]</code> <p>Maximum number of results to return.  <code>None</code> returns all matching items.</p> required <code>dataset_kwargs</code> <code>Optional[Dict[str, Any]]</code> <p>Optional extra configuration forwarded to the server.</p> <code>None</code> <code>threshold</code> <code>Optional[float]</code> <p>Minimum cosine similarity score; items below are dropped.</p> <code>None</code> <code>corrupt_representations</code> <code>bool</code> <p>When <code>True</code>, the server intentionally corrupts item text embeddings before searching.  Used for robustness experiments.</p> <code>False</code> <code>corruption_seed</code> <code>Optional[int]</code> <p>Random seed for reproducible corruption.  Only meaningful when <code>corrupt_representations=True</code>.</p> <code>None</code> <code>allowed_ids</code> <code>Optional[List[str]]</code> <p>Whitelist of item IDs (as strings) to restrict results to.  Items not in this list are excluded from the search.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Any]</code> <p>A <code>(ids, similarities, texts)</code> tuple where <code>ids</code> and</p> <code>List[Optional[float]]</code> <p><code>similarities</code> are parallel lists and <code>texts</code> is a list of item</p> <code>Optional[List[str]]</code> <p>text strings or <code>None</code> if the server did not return text.</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_server.VectorIndex","title":"<code>VectorIndex(df_id, df_text, embeddings, id_to_row, catalog, hnsw_index, feature_descriptions=dict())</code>  <code>dataclass</code>","text":"<p>In-memory HNSW vector index for one dataset.</p> <p>Built once at server startup by :func:<code>_build_index</code> and then used read-only across all request threads.</p> <p>Attributes:</p> Name Type Description <code>df_id</code> <code>ndarray</code> <p>Item IDs in catalog order, shape <code>(N,)</code>.</p> <code>df_text</code> <code>List[str]</code> <p>Item text strings in catalog order, length <code>N</code>.</p> <code>embeddings</code> <code>ndarray</code> <p>L2-normalised embeddings, shape <code>(N, D)</code>.</p> <code>id_to_row</code> <code>Dict[str, int]</code> <p>Maps <code>str(id)</code> to the row index in <code>df_id</code> / <code>embeddings</code> for fast lookup.</p> <code>catalog</code> <code>DataFrame</code> <p>Raw catalog DataFrame (used for column-based scoring).</p> <code>hnsw_index</code> <code>Index</code> <p>hnswlib cosine-space HNSW index over <code>embeddings</code>.</p> <code>feature_descriptions</code> <code>Dict[str, Any]</code> <p>Mapping of column name \u2192 human-readable description, copied from the dataset's <code>true_features</code> dict.</p>"},{"location":"api/tools/vector_search/#coshop.tools.vector_search_server.create_app","title":"<code>create_app(embedding_api_url=None, embedding_model=None)</code>","text":"<p>WSGI app factory for gunicorn.</p> <p>The embedding API URL and model name are read from the EMBEDDING_API_URL and EMBEDDING_MODEL env vars when called without arguments (standard gunicorn factory invocation). Both are required.</p>"},{"location":"api/user_simulator/expert_user/","title":"Expert user","text":"<p>ExpertUser: simulated user with column-level dropout and a growing known-features set.</p> <p>Inherits from LangchainAgent and UserSimulator. Prompts are loaded from simulator_prompts.json.</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser","title":"<code>ExpertUser(spec, dataset, seed=0, max_features_per_turn=5, early_stop_on_xstar=False, search_features=None, experience_features=None, credence_features=None, *args, model_name='gpt-5-nano', model_kwargs={}, max_react_steps=25, randomly_choose_initial=False, initial_dropout_rate=None, use_oracle_item_representations=False, use_actual_item_values=False, use_item_jsons=True, use_structured_actions=False, parser_reasoning_effort='medium', max_text_len=None, proactive_user=False, **kwargs)</code>","text":"<p>               Bases: <code>LangchainAgent</code>, <code>UserSimulator</code></p> <p>LLM-powered user simulator with SEC-aware feature revelation.</p> <p><code>ExpertUser</code> simulates a shopper whose preferences are encoded in a :class:<code>~coshop.data.dataset.Specification</code>.  It uses a LangChain ReAct agent to respond to the shopping assistant and a set of helper classes to track which features have been revealed, parse the assistant's messages, and compare items.</p> <p>Benchmark modes</p> <ul> <li>Natural language mode (<code>use_structured_actions=False</code>, default):   The simulator responds in free-form text, including questions and answers.</li> <li>Structured action mode (<code>use_structured_actions=True</code>):   The simulator responds with structured actions (parsed by   :class:<code>~coshop.user_simulator.helpers.structured_action_message_parser.StructuredActionMessageParser</code>).</li> </ul> <p>Feature revelation</p> <p>Features are drawn from three SEC categories (<code>search_features</code>, <code>experience_features</code>, <code>credence_features</code>). Initially, only <code>search</code> features that pass through the dataset's dropout settings are known.  As the conversation progresses, the simulator may reveal additional features up to <code>max_features_per_turn</code> per turn.</p> <p>Official benchmark settings (as configured by <code>evaluate_agent.py</code>): <code>use_structured_actions=False</code>, <code>max_features_per_turn=5</code>, <code>early_stop_on_xstar=True</code>, <code>proactive_user=False</code>, <code>use_oracle_item_representations=False</code>, <code>use_actual_item_values=False</code>.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>Specification</code> <p>Task specification containing <code>xstar</code>, z-variants, utility function, historical data, and simulator persona.</p> required <code>dataset</code> <p>Dataset instance; provides <code>catalog</code>, <code>true_features</code> (column descriptions), and <code>representation</code>.</p> required <code>seed</code> <code>int</code> <p>Random seed (currently unused; reserved for future use).</p> <code>0</code> <code>max_features_per_turn</code> <code>int</code> <p>Maximum number of new preference features the simulator may reveal in a single turn.  Official setting: <code>5</code>.</p> <code>5</code> <code>early_stop_on_xstar</code> <code>bool</code> <p>If <code>True</code>, the simulator stops the conversation once it believes <code>xstar</code> has been found. Official setting: <code>True</code>.</p> <code>False</code> <code>search_features</code> <code>Optional[List[str]]</code> <p>List of column names classified as Search features for this episode.  If all three SEC lists are <code>None</code>, all feature columns are treated as search features.</p> <code>None</code> <code>experience_features</code> <code>Optional[List[str]]</code> <p>List of column names classified as Experience features (revealed after the user interacts with the product).</p> <code>None</code> <code>credence_features</code> <code>Optional[List[str]]</code> <p>List of column names classified as Credence features (hard-to-verify, revealed last).</p> <code>None</code> <code>model_name</code> <code>str</code> <p>LLM model name for the ReAct agent and all helpers. Defaults to <code>\"gpt-5-nano\"</code>.</p> <code>'gpt-5-nano'</code> <code>model_kwargs</code> <code>dict</code> <p>Extra keyword arguments forwarded to the LLM client (e.g. <code>api_base</code>, <code>reasoning_effort</code>).</p> <code>{}</code> <code>max_react_steps</code> <code>int</code> <p>Maximum ReAct loop iterations per turn.</p> <code>25</code> <code>randomly_choose_initial</code> <code>bool</code> <p>If <code>True</code>, the simulator's initial known features are drawn randomly rather than from <code>spec.initial_known_features</code>.  Use together with <code>initial_dropout_rate</code>.</p> <code>False</code> <code>initial_dropout_rate</code> <code>Optional[float]</code> <p>Fraction of features to drop from the initial feature set when <code>randomly_choose_initial=True</code>.  <code>None</code> means no dropout.</p> <code>None</code> <code>use_oracle_item_representations</code> <code>bool</code> <p>If <code>True</code>, the simulator sees the ground-truth (non-vagueified) item representations when evaluating items shown by the agent.</p> <code>False</code> <code>use_actual_item_values</code> <code>bool</code> <p>If <code>True</code>, the simulator uses exact feature values instead of vagueified ones when describing its preferences.</p> <code>False</code> <code>use_item_jsons</code> <code>bool</code> <p>If <code>True</code>, item representations are formatted as JSON dicts instead of the default paragraph format.</p> <code>True</code> <code>use_structured_actions</code> <code>bool</code> <p>If <code>True</code>, use structured-action mode; otherwise natural-language mode (official default).</p> <code>False</code> <code>parser_reasoning_effort</code> <code>str</code> <p>Reasoning effort level for the response parsing LLM.  One of <code>\"low\"</code>, <code>\"medium\"</code> (default), <code>\"high\"</code>.</p> <code>'medium'</code> <code>max_text_len</code> <code>Optional[int]</code> <p>Maximum character length of item text shown to the simulator.  <code>None</code> disables truncation.</p> <code>None</code> <code>proactive_user</code> <code>bool</code> <p>If <code>True</code>, the simulator proactively hints at features it needs even when the agent does not ask.  Official setting: <code>False</code>.</p> <code>False</code> <code>**kwargs</code> <p>Additional keyword arguments forwarded to :class:<code>~coshop.utils.langchain_agent.LangchainAgent</code>.</p> <code>{}</code>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.get_current_z","title":"<code>get_current_z()</code>","text":"<p>Return the current preference string built from known_features.</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.get_state_history","title":"<code>get_state_history()</code>","text":"<p>Return history of state per turn (z at end of turn, features added by source) plus existing histories.</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.rank_items","title":"<code>rank_items(items, mode='agentic', **kwargs)</code>","text":"<p>Rank a provided list of item_ids using either: - <code>agentic</code>: LLM ranks from item text and conversation context. - <code>parser</code>: LOTUS feature-match on item descriptions vs known features;   score = (# True) / (# active columns); <code>False</code> and <code>None</code> count as fails. - otherwise (e.g. <code>rank</code>): column-based utility (catalog vs xstar) restricted   to known features.</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.rank_items_initial_state","title":"<code>rank_items_initial_state(items, mode='agentic', **kwargs)</code>","text":"<p>Rank items as the simulator would have at the very start of the conversation, using only the initial known features / initial system message (i.e. before any clarifications or feedback updates).</p> <p>Same <code>mode</code> values as :meth:<code>rank_items</code> (<code>agentic</code>, <code>parser</code>, or catalog <code>rank</code>).</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.reset","title":"<code>reset()</code>","text":"<p>Reset the agent to its initial state.</p>"},{"location":"api/user_simulator/expert_user/#coshop.user_simulator.expert_user.ExpertUser.set_budget_tracker","title":"<code>set_budget_tracker(budget_tracker)</code>","text":"<p>Store the budget tracker for checking questions/items limits.</p>"},{"location":"api/user_simulator/helpers/","title":"Helpers","text":"<p>Feature revelation tracker for the user simulator.</p> <p>Manages the SEC (Search / Experience / Credence) feature state for a single elicitation episode: which features are currently known to the agent, which are queued for revelation, and how many may be revealed per turn.</p> <p>Main exports:</p> <ul> <li>:class:<code>Feature</code> \u2014 dataclass describing one preference feature.</li> <li>:class:<code>FeatureTracker</code> \u2014 stateful tracker; updated as the conversation progresses.</li> <li>:func:<code>randomly_init_known_features</code> \u2014 helper to draw an initial random known set.</li> </ul> <p>Natural-language message parser for the user simulator.</p> <p>Parses the shopping agent's response into structured actions that the simulator can act on: clarifying questions, item evaluations, and explanations.</p> <p>Main exports:</p> <ul> <li>:class:<code>ClarifyingQuestion</code> \u2014 a question the agent asked the user.</li> <li>:class:<code>ItemToEval</code> \u2014 an item the agent showed that the simulator should evaluate.</li> <li>:class:<code>Explanation</code> \u2014 an explanation/statement from the agent about a feature.</li> <li>:class:<code>MessageParser</code> \u2014 parses a full agent message into the above types.</li> </ul>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker","title":"<code>FeatureTracker(target_df, search_features, experience_features, credence_features, max_features_to_reveal=None, verbosity=0, column_descriptions=None, item_name=None, explanations_can_reveal_non_queued_features=True)</code>","text":"<p>Initialize the FeatureTracker with the target dataframe and the list of features. If a column is not in the list of search, experience, or credence features, it is considered a hidden feature.</p> <p>Parameters:</p> Name Type Description Default <code>target_df</code> <code>DataFrame</code> <p>this should be from ds.simulator_catalog -&gt; subset to xstar ids -&gt; explode tags</p> required <code>search_features</code> <code>List[str]</code> <p>columns in target_df; this includes columns in the original catalog + tags</p> required <code>experience_features</code> <code>List[str]</code> <p>columns in target_df; this includes columns in the original catalog + tags</p> required <code>credence_features</code> <code>List[str]</code> <p>columns in target_df; this includes columns in the original catalog + tags</p> required <code>max_features_to_reveal</code> <code>int</code> <p>maximum number of features to reveal per item / question</p> <code>None</code> <code>verbosity</code> <code>int</code> <p>if &gt; 0, print feature lists and settings at initialization</p> <code>0</code>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.clear_reveal_history","title":"<code>clear_reveal_history()</code>","text":"<p>Clear the feature reveal history (e.g. on simulator reset).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.get_known_context","title":"<code>get_known_context(relevant_columns=None, drop_na_vals=False)</code>","text":"<p>Returns the context for answering questions and giving feedback. Filters to known features. If relevant_columns is provided, filters to only include known features in the relevant columns.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.get_unknown_feature_types","title":"<code>get_unknown_feature_types(relevant_columns)</code>","text":"<p>For the given column names, return the unknown features grouped by category. Only includes features that are tracked (search/experience/credence) and not yet known.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.get_value","title":"<code>get_value(column_name)</code>","text":"<p>Get the value of a feature.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.is_open_to_anything","title":"<code>is_open_to_anything(column_name)</code>","text":"<p>True if this known feature's target value is the dataset's NA placeholder 'open to anything'.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.process_credence_reveals","title":"<code>process_credence_reveals(explanation)</code>","text":"<p>Reveal any queued credence features based on the assistant message. Returns the number of features revealed and the list of credence features the explanation pertains to. Returns: - number of features revealed - list of credence features that we should now answer questions about - list of credence features that we should acknowledge but which we don't reveal</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.process_feedback_reveals","title":"<code>process_feedback_reveals(item)</code>","text":"<p>Reveal any features based on an item evaluation. Returns:     - the number of features revealed     - a list of newly revealed experience feature column names for this item</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.process_question_reveals","title":"<code>process_question_reveals(question)</code>","text":"<p>Reveal any features based on the question. Returns the number of features revealed and the list of credence features the question pertains to.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.FeatureTracker.reveal_features","title":"<code>reveal_features(column_names, categories)</code>","text":"<p>Reveal a list of features by setting them to known. Returns the number of features revealed.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.feature_tracker.randomly_init_known_features","title":"<code>randomly_init_known_features(feature_tracker, num_features, dropout_rate=0.0)</code>","text":"<p>Randomly reveal a subset of the (search) features as known.</p> <p>Each unknown search feature is independently kept with probability <code>1 - dropout_rate</code>. Only search features are revealed; experience and credence features are left unknown. <code>num_features</code> is currently unused. Returns the number of features revealed.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser","title":"<code>MessageParser(model_name, model_kwargs, verbosity, target_df, catalog, column_descriptions, max_features_to_reveal=None, use_item_jsons=False, use_oracle_item_representations=False, use_actual_item_values=False, representation=None, parser_reasoning_effort='medium', max_text_len=None)</code>","text":"<p>Parses a message from the user or assistant into a structured format. Ideally tags should be exploded out in the catalog first.</p> <p>Parameters:</p> Name Type Description Default <code>target_df</code> <code>DataFrame</code> <p>this should be from ds.simulator_catalog -&gt; subset to xstar ids -&gt; explode tags</p> required <code>catalog</code> <code>DataFrame</code> <p>the original catalog</p> required <code>column_descriptions</code> <code>Dict[str, str]</code> <p>a dict mapping column names to human-readable descriptions</p> required <code>model_name</code> <code>str</code> <p>the name of the model to use</p> required <code>model_kwargs</code> <code>dict</code> <p>a dict of kwargs to pass to the model</p> required <code>verbosity</code> <code>int</code> <p>the verbosity level</p> required <code>use_item_jsons</code> <code>bool</code> <p>if True, expect items as per-line JSON objects (with \"id\" and feature keys) instead of ... tags.</p> <code>False</code> <code>use_oracle_item_representations</code> <code>bool</code> <p>if True, build ItemToEval from catalog only (no LLM calls for item content).</p> <code>False</code> <code>representation</code> <code>Any</code> <p>used with use_oracle for non-JSON raw_description (row_to_str). Required when use_oracle and not use_item_jsons.</p> <code>None</code> <code>max_text_len</code> <code>Optional[int]</code> <p>when use_oracle and not use_item_jsons, truncate row_to_str to this length (match query/query.py).</p> <code>None</code>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser.items_by_id","title":"<code>items_by_id</code>  <code>property</code>","text":"<p>Aggregated view of all parsed items so far, keyed by id.</p> <p>For each feature, we keep the most recent non-None match seen for that item across all messages. A None match never overwrites a non-None one.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser.parsed_history","title":"<code>parsed_history</code>  <code>property</code>","text":"<p>History of parsed messages in order (oldest first).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser.clear_parsed_history","title":"<code>clear_parsed_history()</code>","text":"<p>Clear the parsed message history (e.g. on simulator reset).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser.feature_match_item_descriptions","title":"<code>feature_match_item_descriptions(items, column_names)</code>","text":"<p>Run LOTUS feature match on explicit item descriptions, restricted to the given catalog columns (same row shape as <code>_parse_items</code> / <code>_build_feature_rows</code>). Calls :func:<code>user_simulator.feature_utils.parse_feature_match_with_lotus</code> directly. Does not append to <code>_parsed_history</code>.</p> <p>Returns:</p> Type Description <code>(match_by_item, active_columns)</code> <p><code>active_columns</code> lists columns with a</p> <code>List[str]</code> <p>concrete target (not missing, not <code>OPEN_TO_ANYTHING</code>), in <code>column_names</code> order.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParser.parse","title":"<code>parse(message, question_columns_to_include=None, explanation_columns_to_include=None)</code>","text":"<p>Parse a message from the user or assistant into a structured format. Appends the result to parsed_history (in order).</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>the assistant message to parse.</p> required <code>question_columns_to_include</code> <code>Optional[List[str]]</code> <p>If provided, restrict which catalog columns LOTUS is allowed to select as relevant for the clarifying-question extraction step.</p> <code>None</code>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests","title":"<code>MessageParserHMTests</code>","text":"<p>               Bases: <code>TestCase</code></p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_clarifying_history_bias_and_no_truncation","title":"<code>test_clarifying_history_bias_and_no_truncation()</code>","text":"<p>Clarifying-question mapping should: - keep all relevant columns from LOTUS (no truncation by max_features_to_reveal), - bias toward columns that have not previously been the sole relevant column.</p> <p>We simulate LOTUS behavior via a local stub for sem_map_with_retries.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_explanation_dress_length","title":"<code>test_hm_explanation_dress_length()</code>","text":"<p>Agent explains dress/skirt length options \u2192 one Explanation with dress_length; target value should be in range.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_explanation_none_for_question","title":"<code>test_hm_explanation_none_for_question()</code>","text":"<p>Question only (no teaching) \u2192 no explanations.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_explanation_patterns","title":"<code>test_hm_explanation_patterns()</code>","text":"<p>Agent lists pattern options \u2192 one Explanation for graphical_appearance_name; target_value_mentioned_or_in_range depends on target.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_item_parsing_with_raw_description","title":"<code>test_hm_item_parsing_with_raw_description()</code>","text":"<p>Single item with rich description; ensure id, raw_description, and key features are extracted.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_material_question","title":"<code>test_hm_material_question()</code>","text":"<p>Single clarifying question mapped to 'material'.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_multi_clarifying_questions","title":"<code>test_hm_multi_clarifying_questions()</code>","text":"<p>Multiple clarifying questions with specific expected relevant columns.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_hm_no_items_no_clarifying_questions","title":"<code>test_hm_no_items_no_clarifying_questions()</code>","text":"<p>Strategy-only message: no  tags and only meta questions -&gt; no items, no clarifying questions. <p>Structured item feedback: build (item, feature) rows, run semantic match via LOTUS, then generate per-item feedback (2 sentences) with optional parallel LOTUS call.</p> <p>QuestionAnswerer: answers clarifying questions one-by-one via LOTUS sem_map.</p> <p>Given current preference context (z), a single question, and a hint about which feature(s) matter, generates a short shopper-style answer. Used by ExpertUser to build the full response to multiple clarifying questions by stitching per-question answers.</p> <p>Agentic item ranking for user-simulator evaluation.</p> <p>Provides :func:<code>rank_items_agentic</code>, which asks a copy of the ReAct agent to rank a set of catalog items from best to worst, and :func:<code>create_temp_agent</code>, which clones an agent with modified tools and step limits for the ranking step without altering the original agent state.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.message_parser.MessageParserHMTests.test_parse_items_json_mode","title":"<code>test_parse_items_json_mode()</code>","text":"<p>With use_item_jsons=True, items are parsed from per-line JSON objects.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.item_comparer.ItemComparer","title":"<code>ItemComparer(feature_tracker, true_features, model_name='gpt-5-nano', model_kwargs=None, verbosity=0, *, catalog=None, representation=None, hint_missing_features=False, max_more_info_requests=1, use_actual_item_values=False)</code>","text":"<p>Builds a structured (item, feature) view, runs semantic match via LOTUS, then generates per-item feedback (2 sentences), with an option to batch feedback via LOTUS.</p> <p>Note: \"perfect\" here means that all presented feature columns semantically pass the user's requirements for this item. It does NOT mean an exact match to the user's ideal x*, which is handled separately in <code>expert_user.py</code>.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.item_comparer.ItemComparer.feedback_prompt_history","title":"<code>feedback_prompt_history</code>  <code>property</code>","text":"<p>History of (item_id, prompt, response) for each 2-sentence feedback prompt shown to the simulator.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.item_comparer.ItemComparer.clear_feedback_prompt_history","title":"<code>clear_feedback_prompt_history()</code>","text":"<p>Clear the feedback prompt history (e.g. on simulator reset).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.item_comparer.ItemComparer.compute_feedback","title":"<code>compute_feedback(items_to_evaluate, feedback_fn=None, new_reveals_by_item_id=None)</code>","text":"<p>Compute structured feedback for each item.</p> <p>Parameters:</p> Name Type Description Default <code>items_to_evaluate</code> <code>List[ItemToEval]</code> <p>list of ItemToEval from the message parser.</p> required <code>feedback_fn</code> <code>Optional[Callable[[str], Tuple[str, int, float]]]</code> <p>optional callable (prompt: str) -&gt; (feedback_text: str, token_cost: int, runtime: float). If None, uses LOTUS sem_map to batch-generate feedback.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tuple[Dict[str, str], int, float]</code> <p>(id_to_feedback, total_token_cost, total_runtime_cost).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.question_answerer.QuestionAnswerer","title":"<code>QuestionAnswerer(model_name='gpt-5-nano', model_kwargs=None, true_features=None, simulator_persona='', verbosity=0)</code>","text":"<p>Answers each clarifying question via a separate LOTUS sem_map call, then stitches the results into one response.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.question_answerer.QuestionAnswerer.question_prompt_history","title":"<code>question_prompt_history</code>  <code>property</code>","text":"<p>History of (question, prompt, response) for each clarifying-question answer.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.question_answerer.QuestionAnswerer.answer_questions","title":"<code>answer_questions(questions, z_context, feature_values=None)</code>","text":"<p>Answer each question with a separate LOTUS call, then stitch into one response.</p> <p>Parameters:</p> Name Type Description Default <code>questions</code> <code>List[ClarifyingQuestion]</code> <p>List of clarifying questions (each has .question and .relevant_columns).</p> required <code>z_context</code> <code>str</code> <p>Current preference context (z) for the shopper.</p> required <code>feature_values</code> <code>Optional[Dict[str, str]]</code> <p>Optional mapping column_name -&gt; value to show in the feature hint.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>(stitched_response, token_cost, runtime_cost). Token/runtime from LOTUS</p> <code>int</code> <p>are not available here, so returns 0, 0.0 for cost.</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.question_answerer.QuestionAnswerer.clear_question_prompt_history","title":"<code>clear_question_prompt_history()</code>","text":"<p>Clear the question prompt history (e.g. on simulator reset).</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.final_rank_items.create_temp_agent","title":"<code>create_temp_agent(original_agent, retrieval_function=None, execution_max_per_retrieval=None, execution_max_queries=None, execution_global_max=None, min_react_steps=None, max_react_steps=None, **kwargs)</code>","text":"<p>Create a temporary agent executor with updated query tool for get_final_predictions. This allows using a higher execution_max_per_retrieval value during prediction while keeping the original agent intact.</p> <p>Parameters:</p> Name Type Description Default <code>retrieval_function</code> <code>Optional[Callable]</code> <p>The query function to use</p> <code>None</code> <code>execution_max_per_retrieval</code> <code>Optional[int]</code> <p>The max_items_limit for the new tool</p> <code>None</code> <code>execution_max_queries</code> <code>int</code> <p>Maximum number of queries</p> <code>None</code> <code>execution_global_max</code> <code>Optional[int]</code> <p>Maximum total items that can be retrieved across all query calls</p> <code>None</code> <p>Returns:</p> Type Description <p>A temporary LangChainModel with updated tools, or None if not a LangchainAgent</p>"},{"location":"api/user_simulator/helpers/#coshop.user_simulator.helpers.final_rank_items.rank_items_agentic","title":"<code>rank_items_agentic(original_agent, items, max_retries=2, temperature=0.0, **kwargs)</code>","text":"<p>Ask the agent to rank items (by id) from best to worst using a temp agent with no query function.</p> <p>Parameters:</p> Name Type Description Default <code>original_agent</code> <code>LangChainModel</code> <p>The agent executor to use (state is copied to temp agent).</p> required <code>items</code> <code>Dict[str, str]</code> <p>Dict mapping item id to string representation of that item.</p> required <code>max_retries</code> <code>int</code> <p>Maximum number of retries for getting a valid list of item ids.</p> <code>2</code> <p>Returns:</p> Type Description <code>(ordered_ids, metadata)</code> <p>List of item ids from best to worst, and a dict with \"error\", \"raw\", and/or \"attempt\" on failure; empty metadata on success.</p>"},{"location":"api/user_simulator/user/","title":"User","text":"<p>Abstract base class for user simulators.</p> <p>Defines the interface shared by all user simulator implementations: - :meth:<code>UserSimulator._generate_response</code>: produce the next turn given the assistant's message. - :meth:<code>UserSimulator.rank_items</code>: rank a list of item IDs from best to worst. - :meth:<code>UserSimulator.set_budget_tracker</code>: attach a :class:<code>~coshop.evaluation.budget.BudgetTracker</code>.</p> <p>Checkpoint / state serialisation is handled via :class:<code>~coshop.utils.agent.Agent</code> with an extra <code>specification_state</code> key added by this class.</p>"},{"location":"api/user_simulator/user/#coshop.user_simulator.user.UserSimulator","title":"<code>UserSimulator(spec=None, verbosity=0, checkpoint_file=None, **kwargs)</code>","text":"<p>               Bases: <code>Agent</code></p> <p>Abstract base class for all user simulators.</p> <p>A user simulator plays the role of the shopper in a conversational recommendation episode.  Concrete subclasses must implement :meth:<code>_generate_response</code> (produce the next utterance given the agent's message) and :meth:<code>rank_items</code> (rank candidate items from best to worst).</p> <p>The simulator holds a :class:<code>~coshop.data.dataset.Specification</code> which encodes the user's ground-truth preferences (<code>xstar</code>, z-variants, utility function, etc.) and exposes <code>item_name</code> and <code>simulator_persona</code> for prompt construction.</p> <p>Initialize the user simulator.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>Optional[Specification]</code> <p>The specification containing reward / validation function (can be passed as positional or keyword arg)</p> <code>None</code> <code>verbosity</code> <code>Literal[0, 1, 2]</code> <p>Whether to print verbose output. 0: print nothing, 1: print function outputs but not prompts, 2: print everything</p> <code>0</code> <code>checkpoint_file</code> <code>Optional[str]</code> <p>File to save checkpoints</p> <code>None</code> <code>**kwargs</code> <p>Additional keyword arguments</p> <code>{}</code>"},{"location":"api/user_simulator/user/#coshop.user_simulator.user.UserSimulator.rank_items","title":"<code>rank_items(items, mode='agentic', **kwargs)</code>","text":"<p>Rank a provided mapping of item ID -&gt; item text from best to worst, according to the simulator's notion of user preferences.</p> <p>Subclasses must implement this; the base class only defines the interface so that downstream evaluation code can call simulator.rank_items(...) in a uniform way.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>A <code>(ranked_ids, metadata)</code> tuple where <code>ranked_ids</code> orders the</p> <code>Dict[str, Any]</code> <p>keys of <code>items</code> from best to worst and <code>metadata</code> holds</p> <code>Tuple[List[str], Dict[str, Any]]</code> <p>mode-specific details (e.g. scores, known features).</p>"},{"location":"api/user_simulator/user/#coshop.user_simulator.user.UserSimulator.rank_items_initial_state","title":"<code>rank_items_initial_state(items, mode='agentic', **kwargs)</code>","text":"<p>Rank a provided mapping of item ID -&gt; item text using the simulator's initial state (i.e., before any interaction has taken place).</p> <p>By default, this falls back to the current-state ranking behavior so existing simulators continue to work without modification. Subclasses can override this to provide true initial-state ranking.</p>"},{"location":"api/user_simulator/user/#coshop.user_simulator.user.UserSimulator.set_budget_tracker","title":"<code>set_budget_tracker(budget_tracker)</code>","text":"<p>Set the budget tracker for simulators that support budget-aware behavior. No-op by default; subclasses (e.g. ZstarSingleTurnBudgeted) override to store it.</p>"},{"location":"api/utils/agent/","title":"Agent","text":"<p>Base Agent class for all conversational participants in coshop.</p> <p>Provides conversation-history tracking, checkpoint save/restore, and a hook system (pre/post-generation) shared by both the shopping-assistant policy and the user simulator.</p>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent","title":"<code>Agent(verbosity=0, pre_conversation_hooks=[], pre_generation_hooks=[], post_generation_hooks=DEFAULT_POST_GENERATION_HOOKS, checkpoint_file=None, **kwargs)</code>","text":"<p>Base class for agents that participate in conversations. Provides common conversation tracking functionality.</p> <p>Initialize the agent.</p> <p>Parameters:</p> Name Type Description Default <code>verbosity</code> <code>Literal[0, 1, 2]</code> <p>Whether to print verbose output. 0: print nothing, 1: print function outputs but not prompts, 2: print everything</p> <code>0</code> <code>pre_conversation_hooks</code> <code>List[Union[str, Callable]]</code> <p>Hooks to run before processing the first input message (only runs once)</p> <code>[]</code> <code>pre_generation_hooks</code> <code>List[Union[str, Callable]]</code> <p>Hooks to run before generating response</p> <code>[]</code> <code>post_generation_hooks</code> <code>List[Union[str, Callable]]</code> <p>Hooks to run after generating response</p> <code>DEFAULT_POST_GENERATION_HOOKS</code> <code>checkpoint_file</code> <code>Optional[str]</code> <p>File to save checkpoints. If None, checkpointing is disabled.</p> <code>None</code>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.turn_count","title":"<code>turn_count</code>  <code>property</code>","text":"<p>Index of the current turn in the conversation.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The index of the current turn</p>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.get_action_history","title":"<code>get_action_history()</code>","text":"<p>Get the action history.</p> <p>Returns:</p> Type Description <code>Dict[int, List[Dict]]</code> <p>Dict[int, List[Dict]]: Dictionary mapping turn numbers to lists of action dictionaries</p>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.get_conversation_history","title":"<code>get_conversation_history()</code>","text":"<p>Get the conversation history as a list of dicts.</p> <p>Returns:</p> Type Description <code>List[dict]</code> <p>List[dict]: List of conversation turns as dictionaries</p>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.load_checkpoint","title":"<code>load_checkpoint(checkpoint_file=None, turn_idx=None, connection=None)</code>","text":"<p>Load the agent state from a checkpoint file. Critically, this does not save configs for the agent, so the agent must be initialized with the same configs as the checkpoint.</p> <p>Relies on the hook history to load the state by turn.</p> <p>Parameters:</p> Name Type Description Default <code>checkpoint_file</code> <code>Optional[str]</code> <p>Path to checkpoint file. If None, uses self.checkpoint_file.</p> <code>None</code> <code>turn_idx</code> <code>Optional[int]</code> <p>If provided, only load the state up to this turn. If None, load the entire checkpoint.</p> <code>None</code> <code>connection</code> <p>Optional connection object for remote file access</p> <code>None</code>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.reset","title":"<code>reset()</code>","text":"<p>Reset the agent to its initial state.</p>"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.run_hooks","title":"<code>run_hooks(hooks, *args, **kwargs)</code>","text":"<p>Run the hooks.</p> <p>Parameters:</p> Name Type Description Default <code>hooks</code> <code>List[Union[str, Callable]]</code> <p>List of hooks to run (can be strings or callables)</p> required"},{"location":"api/utils/agent/#coshop.utils.agent.Agent.save_checkpoint","title":"<code>save_checkpoint(connection=None)</code>","text":"<p>Save the current state of the agent to a checkpoint file. Critically, this does not save configs for the agent, so the agent later must be initialized with the same configs as the checkpoint.</p> <p>Parameters:</p> Name Type Description Default <code>connection</code> <p>Optional connection object for remote file access</p> <code>None</code>"},{"location":"api/utils/custom_types/","title":"Custom types","text":"<p>Type definitions and I/O helpers for elicitation interactions.</p> <p>Defines the dataclass hierarchy used to record, serialise, and reload complete elicitation episodes:</p> <ul> <li>:class:<code>ToolCall</code> \u2014 a single tool invocation within an agent action.</li> <li>:class:<code>Action</code> \u2014 one step of a ReAct loop (content + tool calls).</li> <li>:class:<code>ConversationTurn</code> \u2014 a full message turn with runtime/token costs.</li> <li>:class:<code>ElicitationTurn</code> \u2014 paired (user, policy) turn with per-side costs.</li> <li>:class:<code>ItemEvaluationInfo</code> \u2014 per-item utility scores for one evaluation.</li> <li>:class:<code>SeenEvaluation</code> / :class:<code>PredictedEvaluation</code> / :class:<code>Evaluation</code>   \u2014 structured evaluation results.</li> <li>:class:<code>ElicitationInteraction</code> \u2014 top-level container for a complete episode.</li> </ul> <p>Utility functions:</p> <ul> <li>:func:<code>save_elicitation_interaction</code> \u2014 build and persist an   :class:<code>ElicitationInteraction</code> to JSON.</li> <li>:func:<code>load_interaction</code> \u2014 load a saved interaction from JSON.</li> <li>:func:<code>conversation_history_to_messages</code> \u2014 flatten turns into a   <code>[{\"role\": ..., \"content\": ...}]</code> message list.</li> </ul>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.Action","title":"<code>Action(content, tool_calls, status)</code>  <code>dataclass</code>","text":"<p>Represents an action in a conversation turn</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.ConversationTurn","title":"<code>ConversationTurn(msg, actions, runtime_cost, token_cost)</code>  <code>dataclass</code>","text":"<p>Represents a turn in a conversation</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.ElicitationInteraction","title":"<code>ElicitationInteraction(turns, config, budget_metrics, end_reason, filename, retrieval_type, corrupt_representations, policy, policy_model, representation_type='paragraph', spec_information=dict(), elicitation_evaluation=None, research_evaluation=None, elicitation_simulator_evaluation_oracle=None, elicitation_simulator_evaluation_oracle_initial_state=None, elicitation_simulator_evaluation_reports=None, elicitation_simulator_evaluation_reports_initial_state=None, research_simulator_evaluation_oracle=None, research_simulator_evaluation_oracle_initial_state=None, research_simulator_evaluation_reports=None, research_simulator_evaluation_reports_initial_state=None, policy_checkpoint_file=None, simulator_checkpoint_file=None, final_prediction_metadata=dict(), extra_metadata=dict())</code>  <code>dataclass</code>","text":"<p>Represents a complete elicitation interaction and its evaluation</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.ElicitationTurn","title":"<code>ElicitationTurn(user_msg, user_actions, user_token_cost, user_runtime_cost, policy_msg, policy_actions, policy_token_cost, policy_runtime_cost)</code>  <code>dataclass</code>","text":"<p>Represents a single turn in an interaction</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.Evaluation","title":"<code>Evaluation(seen, predicted, execution_max_per_retrieval, execution_max_queries, execution_global_max, k)</code>  <code>dataclass</code>","text":"<p>Represents evaluation results for an elicitation process with seen and predicted sections</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.ItemEvaluationInfo","title":"<code>ItemEvaluationInfo(id, column_ustar, embedding_ustar, em, ustar=None)</code>  <code>dataclass</code>","text":"<p>Information about a single item in evaluation</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.PredictedEvaluation","title":"<code>PredictedEvaluation(predicted_ids, max_column_ustar_at_k, avg_column_ustar_at_k, max_embedding_ustar_at_k, avg_embedding_ustar_at_k, max_ustar_at_k, avg_ustar_at_k, column_ustar_at_1, embedding_ustar_at_1, ustar_at_1, ndcg_column_ustar_at_k, ndcg_embedding_ustar_at_k, ndcg_ustar_at_k, ndcg_binary_at_k, recall_at_k, recall_at_1)</code>  <code>dataclass</code>","text":"<p>Evaluation metrics for predicted items</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.SeenEvaluation","title":"<code>SeenEvaluation(seen_ids, max_column_ustar, avg_column_ustar, max_embedding_ustar, avg_embedding_ustar, recall_at_seen, max_ustar=None, avg_ustar=None)</code>  <code>dataclass</code>","text":"<p>Evaluation metrics for seen items</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.ToolCall","title":"<code>ToolCall</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Represents a tool call in an action</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.conversation_history_to_messages","title":"<code>conversation_history_to_messages(conversation_history)</code>","text":"<p>Convert a list of Turn objects to a list of messages in {\"role\": \"assistant\" | \"user\", \"content\": str, \"response_time\": float, \"tool_calls\": List[ToolCall]} format.</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.load_interaction","title":"<code>load_interaction(path, connection=None)</code>","text":"<p>Load the results of an interaction evaluation from a file as an ElicitationInteraction object. Properly reconstructs all dataclass objects from JSON data.</p>"},{"location":"api/utils/custom_types/#coshop.utils.custom_types.save_elicitation_interaction","title":"<code>save_elicitation_interaction(*, simulator, policy, output_path, spec, config, end_reason, budget_metrics, elicitation_evaluation=None, research_evaluation=None, connection=None, **kwargs)</code>","text":"<p>Save the elicitation interaction and evaluation results.</p> <p>Parameters:</p> Name Type Description Default <code>simulator</code> <p>The simulator instance</p> required <code>policy</code> <p>The policy instance</p> required <code>output_path</code> <code>str</code> <p>Path to save results</p> required <code>config</code> <code>dict</code> <p>Configuration</p> required <code>end_reason</code> <code>str</code> <p>The reason for the end of the interaction</p> required <code>budget_metrics</code> <code>Dict[str, Any]</code> <p>Budget tracking metrics</p> required <code>elicitation_evaluation</code> <code>Optional[Evaluation]</code> <p>Elicitation evaluation results</p> <code>None</code> <code>research_evaluation</code> <code>Optional[Evaluation]</code> <p>Research evaluation results</p> <code>None</code> <code>spec</code> <p>Specification instance</p> required <code>**kwargs</code> <p>Additional arguments</p> <code>{}</code> <p>Returns:     An ElicitationInteraction instance</p>"},{"location":"api/utils/langchain_agent/","title":"LangChain agent","text":"<p>LangChain-based agent implementation for coshop.</p> <p>Extends :class:<code>~coshop.utils.agent.Agent</code> with a LangChain ReAct executor, token/runtime tracking, and helper methods for copying agent state to temporary agents (used during final item ranking).</p>"},{"location":"api/utils/langchain_agent/#coshop.utils.langchain_agent.LangchainAgent","title":"<code>LangchainAgent(actions=[], model_name='gpt-5-nano', model_kwargs={'reasoning_effort': 'minimal'}, max_react_steps=25, verbosity=0, **kwargs)</code>","text":"<p>               Bases: <code>Agent</code></p> <p>Base class for agents that use LangChain models. Provides common LangChain functionality.</p> <p>Initialize the LangChain agent.</p> <p>Parameters:</p> Name Type Description Default <code>actions</code> <code>List[StructuredTool]</code> <p>List of tools/actions available to the agent (must be set before calling this)</p> <code>[]</code> <code>model_name</code> <code>str</code> <p>Name of the model to use</p> <code>'gpt-5-nano'</code> <code>model_kwargs</code> <code>dict</code> <p>Additional keyword arguments for the model</p> <code>{'reasoning_effort': 'minimal'}</code> <code>max_react_steps</code> <code>int</code> <p>Maximum number of ReAct steps</p> <code>25</code> <code>verbosity</code> <code>Literal[0, 1, 2]</code> <p>Whether to print verbose output</p> <code>0</code> <code>**kwargs</code> <p>Additional arguments passed to parent class</p> <code>{}</code>"},{"location":"api/utils/langchain_agent/#coshop.utils.langchain_agent.LangchainAgent.cumulative_token_breakdown","title":"<code>cumulative_token_breakdown</code>  <code>property</code>","text":"<p>Cumulative token usage across every LLM call this agent makes \u2014 conversation turns, final-prediction / report calls, and summarizer / compression calls. Counted at the model layer (LangChainModel) and read through here; prediction/report agents share the executor's counter, so their usage is included automatically. Keys: \"input\", \"input_cached\", \"output\", \"reasoning\".</p>"},{"location":"api/utils/langchain_agent/#coshop.utils.langchain_agent.LangchainAgent.insert_message","title":"<code>insert_message(role, content, persist_state=True)</code>","text":"<p>Insert a message into the conversation history without making an LLM call.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>Message role (\"system\", \"user\", \"assistant\", or \"tool\")</p> required <code>content</code> <code>str</code> <p>Message content</p> required <code>persist_state</code> <code>bool</code> <p>If True, appends the message to the graph state</p> <code>True</code>"},{"location":"api/utils/langchain_agent/#coshop.utils.langchain_agent.LangchainAgent.reset","title":"<code>reset()</code>","text":"<p>Reset the agent to its initial state.</p>"},{"location":"api/utils/langchain_agent/#coshop.utils.langchain_agent.parse_langchain_response_to_actions","title":"<code>parse_langchain_response_to_actions(raw)</code>","text":"<p>Parse the raw response from a LangChain model into a list of Action objects.</p> <p>Parameters:</p> Name Type Description Default <code>raw</code> <code>List[Any]</code> <p>List of BaseMessage objects from LangChain</p> required <p>Returns:</p> Type Description <code>List[Action]</code> <p>List of Action objects with content, tool_calls, and status</p>"},{"location":"api/utils/misc/","title":"Misc","text":"<p>Miscellaneous utilities shared across coshop.</p> <p>Includes:</p> <ul> <li>:class:<code>Stopwatch</code> \u2014 wall-clock timer.</li> <li>:func:<code>parse_json</code> / :func:<code>parse_for_answer_tags</code> \u2014 LLM output parsers.</li> <li>:func:<code>print_debug</code> \u2014 verbosity-gated debug printer.</li> <li>:func:<code>strip_thinking_tokens</code> \u2014 removes <code>&lt;think&gt;</code> blocks from model output.</li> <li>JSON serialisation helper: :func:<code>_clean_for_json</code>.</li> </ul>"},{"location":"api/utils/misc/#coshop.utils.misc.Stopwatch","title":"<code>Stopwatch</code>","text":"<p>Context manager for timing a block of code Source: https://stackoverflow.com/questions/33987060/python-context-manager-that-measures-time</p>"},{"location":"api/utils/misc/#coshop.utils.misc.check_if_file_ending_exists","title":"<code>check_if_file_ending_exists(ending, output_dir)</code>","text":"<p>Check if a file of the format {output_dir}/*{ending} exists.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.check_na","title":"<code>check_na(value, allow_empty=False)</code>","text":"<p>Check if a value is NA (handles pd.NA, np.nan, None, and list containing any).</p>"},{"location":"api/utils/misc/#coshop.utils.misc.download_file_from_google_drive","title":"<code>download_file_from_google_drive(file_id, output_path, unzip=False, chunk_size=8192, timeout=600)</code>","text":"<p>Download a file from Google Drive.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.explode_df","title":"<code>explode_df(df, col_to_explode, drop_original=True)</code>","text":"<p>Explode a column that contains an iterable (e.g. lists of strings or a dictionary) The new dataframe will have one column per element of the iterable</p> <p>For dicts, we only care about the keys.</p> <p>Example: df = pd.DataFrame({\"tags\": [[\"new\", \"2019\"], [\"old\", \"2020\"]]}) new_df = explode_df(df, \"tags\") print(new_df)</p>"},{"location":"api/utils/misc/#coshop.utils.misc.explode_df--output","title":"Output:","text":""},{"location":"api/utils/misc/#coshop.utils.misc.explode_df--new-old-2019-2020","title":"new old 2019 2020","text":""},{"location":"api/utils/misc/#coshop.utils.misc.explode_df--0-true-false-true-false","title":"0   True False True False","text":""},{"location":"api/utils/misc/#coshop.utils.misc.explode_df--1-false-true-false-true","title":"1   False True False True","text":"<p>If a tag has the same name as a column in the dataframe, we prioritize the column and ignore the tag.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.hash","title":"<code>hash(x, type='md5')</code>","text":"<p>Hash an object.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.is_same_image","title":"<code>is_same_image(image1, image2)</code>","text":"<p>Check if two images are the same.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.parse_for_answer_tags","title":"<code>parse_for_answer_tags(text, keyword='answer', return_start_end=False, return_none_if_not_found=False, return_all=False)</code>","text":"<p>Looks for X in text and returns X.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.parse_json","title":"<code>parse_json(json_str, return_start_end=False)</code>","text":"<p>Parse a JSON string, returning None if it fails. If there are multiple JSONs in the string, returns the first one. If return_start_end is True, also returns a tuple of (start, end) of the JSON string.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.parse_set","title":"<code>parse_set(value, separator=', ')</code>","text":"<p>Parse a value into a set of strings for set-based comparison (e.g. Jaccard). - str: split on separator (\"x, y, z\" -&gt; {\"x\", \"y\", \"z\"}).   If str looks like a dict/list (starts with '{' or '['), eval and recurse. - dict: use keys as the set (e.g. {\"tag_a\": 1, \"tag_b\": 2} -&gt; {\"tag_a\", \"tag_b\"}). - list/tuple: convert elements to strings. - None/NaN: return empty set.</p>"},{"location":"api/utils/misc/#coshop.utils.misc.print_debug","title":"<code>print_debug(message, function_name, color='NONE', indent_level=0)</code>","text":"<p>Print a debug message with function context and color.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>The message to print (can be multiline)</p> required <code>function_name</code> <code>str</code> <p>Name of the function generating the message</p> required <code>color</code> <code>str</code> <p>Color to use for the function name (default: \"NONE\")</p> <code>'NONE'</code> <code>indent_level</code> <code>int</code> <p>Number of indentation levels (default: 0)</p> <code>0</code>"},{"location":"api/utils/misc/#coshop.utils.misc.strip_thinking_tokens","title":"<code>strip_thinking_tokens(text, start_token='&lt;think&gt;', end_token='&lt;/think&gt;')</code>","text":"<p>Remove ... blocks from text (e.g. before showing response to user).</p>"},{"location":"api/utils/model/","title":"Model","text":"<p>Model wrappers for LLM and embedding APIs used across coshop.</p> <p>Main exports:</p> <ul> <li>:class:<code>LangChainModel</code> \u2014 wraps a LangChain chat model with tool-calling   support and a <code>copy_for_prediction</code> helper.</li> <li>:class:<code>EmbeddingModelWrapper</code> \u2014 wraps a <code>SentenceTransformer</code> or a remote   embedding API with disk caching.</li> <li>Helper functions: :func:<code>get_token_usage</code>, :func:<code>is_openai_model</code>,   :func:<code>is_anthropic_model</code>, :func:<code>is_gemini_model</code>.</li> </ul>"},{"location":"api/utils/model/#coshop.utils.model.EmbeddingModelWrapper","title":"<code>EmbeddingModelWrapper(model_name, cache_dir=None, use_cache=True, max_cache_size_mb=None, max_cache_files=None, quantize=False, batch_size=128, cache_queries=False, device_map=None, embedding_api_url=None)</code>","text":"<p>Wrapper for embedding models to handle different encoding methods. Supports standard SentenceTransformer models, Qwen3-Embedding, and EmbeddingGemma models.</p> <p>Note: Qwen3-Embedding models require transformers&gt;=4.51.0 and sentence-transformers&gt;=2.7.0. EmbeddingGemma models work with standard sentence-transformers installation.</p> <p>Includes disk caching for embeddings to avoid recomputing the same texts.</p> <p>Initialize the embedding model wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of the model (e.g., \"all-MiniLM-L6-v2\", \"Qwen/Qwen3-Embedding-0.6B\", \"google/embeddinggemma-300m\")</p> required <code>cache_dir</code> <code>Optional[str]</code> <p>Optional directory for caching embeddings. If None, uses \".cache/embeddings/\"</p> <code>None</code> <code>use_cache</code> <code>bool</code> <p>Whether to use disk caching for embeddings (default: True)</p> <code>True</code> <code>max_cache_size_mb</code> <code>Optional[float]</code> <p>Maximum cache size in MB. If None, defaults to 1024 MB (1 GB). Set to float('inf') for no limit.</p> <code>None</code> <code>max_cache_files</code> <code>Optional[int]</code> <p>Maximum number of cache files. If None, no file count limit (default: None)</p> <code>None</code> <code>quantize</code> <code>Optional[bool]</code> <p>Quantization level for the model. If None, no quantization is used.</p> <code>False</code> <code>batch_size</code> <code>int</code> <p>Batch size for encoding. Default is 128 (increased from SentenceTransformer's default of 32)</p> <code>128</code> <code>cache_queries</code> <code>bool</code> <p>Whether to cache query embeddings (default: False)</p> <code>False</code> <code>device_map</code> <code>Optional[str]</code> <p>Device map for multi-GPU (e.g. \"auto\"). Passed to SentenceTransformer via model_kwargs.</p> <code>None</code> <code>embedding_api_url</code> <code>Optional[str]</code> <p>If set, use OpenAI-compatible API (e.g. vLLM) instead of loading model locally.</p> <code>None</code>"},{"location":"api/utils/model/#coshop.utils.model.EmbeddingModelWrapper.encode","title":"<code>encode(texts, show_progress_bar=False, **kwargs)</code>","text":"<p>Encode texts into embeddings with caching. Handles different model types appropriately.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <p>List of texts or single text to encode</p> required <code>show_progress_bar</code> <code>bool</code> <p>Whether to show progress bar</p> <code>False</code> <code>**kwargs</code> <p>Additional arguments passed to the encoding method</p> <code>{}</code> <p>Returns:</p> Type Description <p>numpy array of embeddings</p>"},{"location":"api/utils/model/#coshop.utils.model.EmbeddingModelWrapper.encode_query","title":"<code>encode_query(texts, show_progress_bar=False, **kwargs)</code>","text":"<p>Encode queries into embeddings with caching. Uses model-specific query encoding when available.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <p>List of texts or single text to encode as queries</p> required <code>show_progress_bar</code> <code>bool</code> <p>Whether to show progress bar</p> <code>False</code> <code>**kwargs</code> <p>Additional arguments passed to the encoding method</p> <code>{}</code> <p>Returns:</p> Type Description <p>numpy array of embeddings</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel","title":"<code>LangChainModel(model_name, model_no_tools=None, summary_model_name=None, tools=None, verbosity=0, max_react_steps=25, min_react_steps=1, prompt_cache=True, multiturn_memory=False, summarize_state_after=None, out_of_steps_msg=None, list_tools_in_prompt=False, thinking_tokens=('&lt;think&gt;', '&lt;/think&gt;'), add_thinking_tag=True, empty_message_filler=None, api_key=None, model_provider=None, summary_api_key=None, summary_model_provider=None, summary_vllm_api_url=None, **kwargs)</code>","text":"<p>               Bases: <code>Model</code></p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.raw_state","title":"<code>raw_state</code>  <code>property</code>","text":"<p>Return the raw state of the chain (read-only) The raw-state only appends: no summarization, no deletion The exception is if multiturn_memory is False: then the raw_state will be [].</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.state","title":"<code>state</code>  <code>property</code>","text":"<p>Return the state of the chain</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.clear_state","title":"<code>clear_state()</code>","text":"<p>Clear the state of the chain</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.compress_state","title":"<code>compress_state()</code>","text":"<p>Compress the persisted chain by summarizing each tool result (see _compress_messages), then write the result back to the checkpointer. Final AI outputs are left untouched.</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.copy_for_prediction","title":"<code>copy_for_prediction(*, tools, min_react_steps=None, max_react_steps=None)</code>","text":"<p>Create a copy of this agent with different tools and/or react step limits. Preserves all other constructor arguments (model_provider, api_key, etc.). Note this new agent has an empty state. For prediction-time agents (e.g., agentic ranking or final recommendations), we force temperature=0.0 for more deterministic behavior, regardless of the interactive agent's temperature.</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.deduplicate_msgs_by_content","title":"<code>deduplicate_msgs_by_content(contents)</code>","text":"<p>Delete all but the last occurrence of any message whose content is in <code>contents</code>.</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.get_state","title":"<code>get_state(slice=None)</code>","text":"<p>Dump the state of the chain</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.insert_message","title":"<code>insert_message(role, content, persist_state=True)</code>","text":"<p>Insert a single message into the current conversation state.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>str</code> <p>One of \"system\", \"user\", \"assistant\", or \"tool\"</p> required <code>content</code> <code>str</code> <p>Message text content</p> required <code>persist_state</code> <code>bool</code> <p>If True, appends the message to the graph state; if False, no-op to state but returns the constructed message</p> <code>True</code> <p>Returns:</p> Type Description <p>The created LangChain BaseMessage</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.load_state","title":"<code>load_state(state)</code>","text":"<p>Load the state of the chain</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.mark_history_cache_breakpoint","title":"<code>mark_history_cache_breakpoint()</code>","text":"<p>Place an Anthropic prompt-cache breakpoint on the last message of the current state, so the whole conversation-history prefix is cached and re-read across subsequent generate() calls that share this history (e.g. prediction retries, the foregone-recall continuation, and the report/rank passes).</p> <p>cache_control must live inside a content block, so a string-content message is converted to a single text block carrying the breakpoint, and a list-content message gets the breakpoint attached to its last text block. Returns False (no-op) for non-Anthropic models, when prompt caching is disabled, when the state is empty, or when no suitable text block is found. Stays within Anthropic's 4-breakpoint cap: it marks a single message and never accumulates across turns.</p>"},{"location":"api/utils/model/#coshop.utils.model.LangChainModel.reset_token_tracking","title":"<code>reset_token_tracking()</code>","text":"<p>Zero the cumulative token counters (in place, to preserve any shared references held by prediction-time agent copies).</p>"},{"location":"api/utils/model/#coshop.utils.model.Model","title":"<code>Model(name)</code>","text":""},{"location":"api/utils/model/#coshop.utils.model.Model.fmt_as_dialog","title":"<code>fmt_as_dialog(prompts=None, dialogs=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>prompts</code> <code>List[Union[str, Image]]</code> <p>A list of prompts. The list dimension is over (B,) e.g. [\"What is the capital of France?\"] -&gt; [[{\"role\": \"user\", \"content\": \"What is the capital of France?\"}]]</p> <code>None</code> <code>dialogs</code> <code>List[List[Tuple[str, Union[str, Image]]]]</code> <p>A list of [(role, content)] pairs. The list dimension is over (B, D) e.g. [[(\"user\", \"What is the capital of France?\"), (\"assistant\", \"Paris.\")]] -&gt; [[{\"role\": \"user\", \"content\": \"What is the capital of France?\"}, {\"role\": \"assistant\", \"content\": \"Paris.\"}]]</p> <code>None</code> <p>Returns:     Formatted dialogs: list of list of dictionaries (B, D) where each dictionary has keys \"role\" and \"content\"</p>"},{"location":"api/utils/model/#coshop.utils.model.Model.generate","title":"<code>generate(*, prompts=None, dialogs=None, **kwargs)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>prompts</code> <code>List[str]</code> <p>A list of prompts. The list dimension is over (B,) e.g. [\"What is the capital of France?\"]</p> <code>None</code> <code>dialogs</code> <code>List[List[Tuple[str, str]]]</code> <p>A list of [(role, content)] pairs. The list dimension is over (B, D) e.g. [[(\"user\", \"What is the capital of France?\"), (\"assistant\", \"Paris.\")]]</p> <code>None</code> <p>Returns:     A list of completions. The list dimension is over (B,)     e.g. [\"Paris.\"]</p>"},{"location":"api/utils/model/#coshop.utils.model.encode_image_as_user_msg","title":"<code>encode_image_as_user_msg(image=None, image_path=None, extension='png', caption=None, model_name='gpt-5-nano')</code>","text":"<p>Encode an image to a base64 string.</p>"},{"location":"api/utils/model/#coshop.utils.model.get_reasoning_effort_kwargs","title":"<code>get_reasoning_effort_kwargs(model_name, reasoning_effort, max_tokens=64000)</code>","text":"<p>Get the kwargs for the reasoning effort for a model.</p>"},{"location":"api/utils/model/#coshop.utils.model.get_token_breakdown","title":"<code>get_token_breakdown(msg)</code>","text":"<p>Extract per-message token usage broken down into four buckets.</p> Buckets <ul> <li>input: total prompt tokens sent to the model (cached + uncached)</li> <li>input_cached: subset of input that was served from prompt cache</li> <li>output: total tokens generated by the model (includes reasoning for OpenAI)</li> <li>reasoning: subset of output tokens used for hidden reasoning (if any)</li> </ul> <p>Prefers LangChain's normalized <code>usage_metadata</code> (uniform across OpenAI, Anthropic, Gemini). Falls back to provider-raw <code>response_metadata</code> when <code>usage_metadata</code> is unavailable. Returns zeros if neither is present (e.g. local HF/vLLM models that do not report usage).</p>"},{"location":"api/utils/model/#coshop.utils.model.get_token_usage","title":"<code>get_token_usage(response_metadata, default_value=0, return_reasoning_tokens=True)</code>","text":"<p>Get the token usage from the response metadata. If return_reasoning_tokens is True, return completion_tokens + reasoning_tokens. Otherwise, return just the completion tokens.</p>"},{"location":"api/utils/model/#coshop.utils.model.init_langchain_model","title":"<code>init_langchain_model(model_name, **kwargs)</code>","text":"<p>Initialize a LangChain chat model. Args:     model_name: The name of the model to initialize.     **kwargs: Additional keyword arguments to pass to the model. Returns:     A LangChain model.</p>"},{"location":"api/utils/model/#coshop.utils.model.is_anthropic_model","title":"<code>is_anthropic_model(model_name, api_key=None)</code>  <code>cached</code>","text":"<p>Check if a model is an Anthropic model. Args:     model_name: The name of the model to check.     api_key: Optional API key for Anthropic client. Returns:     True if the model is an Anthropic model, False otherwise.</p>"},{"location":"api/utils/model/#coshop.utils.model.is_openai_model","title":"<code>is_openai_model(model_name, api_key=None)</code>  <code>cached</code>","text":"<p>Check if a model is an OpenAI model. Args:     model_name: The name of the model to check.     api_key: Optional API key for OpenAI client. Returns:     True if the model is an OpenAI model, False otherwise.</p>"}]}